"use strict";
(self["webpackChunkpush_chain_website"] = self["webpackChunkpush_chain_website"] || []).push([[74609],{

/***/ 684876
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"label":"label_NYkD"});
    if(true) {
      (function() {
        var localsJsonString = "{\"label\":\"label_NYkD\"}";
        // 1788591701566
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 614649
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"verified":"verified_XaUV"});
    if(true) {
      (function() {
        var localsJsonString = "{\"verified\":\"verified_XaUV\"}";
        // 1788591702703
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 871660
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_sTw3"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_sTw3\"}";
        // 1788591702716
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 495489
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_IExM","article":"article_pepT"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_IExM\",\"article\":\"article_pepT\"}";
        // 1788591702697
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 592289
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"header":"header_RYYj","avatar":"avatar_K4HU","avatarOverflow":"avatarOverflow_VGa3","avatarSquare":"avatarSquare_Zzxq","author":"author_jsWI","authorText":"authorText_dUjv","username":"username_Jiht"});
    if(true) {
      (function() {
        var localsJsonString = "{\"header\":\"header_RYYj\",\"avatar\":\"avatar_K4HU\",\"avatarOverflow\":\"avatarOverflow_VGa3\",\"avatarSquare\":\"avatarSquare_Zzxq\",\"author\":\"author_jsWI\",\"authorText\":\"authorText_dUjv\",\"username\":\"username_Jiht\"}";
        // 1788591702715
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 492595
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"skeleton":"skeleton_FMR8","loading":"loading_FKdv"});
    if(true) {
      (function() {
        var localsJsonString = "{\"skeleton\":\"skeleton_FMR8\",\"loading\":\"loading_FKdv\"}";
        // 1788591701567
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 886649
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"actions":"actions_jOEJ","like":"like_AZSs","reply":"reply_PYbJ","copy":"copy_npTP","likeIconWrapper":"likeIconWrapper_KHFx","likeCount":"likeCount_b04X","replyIconWrapper":"replyIconWrapper_jCup","copyIconWrapper":"copyIconWrapper_oEVy","likeIcon":"likeIcon_gbwt","replyIcon":"replyIcon_ipa5","copyIcon":"copyIcon_IhOq","replyText":"replyText_jXTI","copyText":"copyText_vRl0"});
    if(true) {
      (function() {
        var localsJsonString = "{\"actions\":\"actions_jOEJ\",\"like\":\"like_AZSs\",\"reply\":\"reply_PYbJ\",\"copy\":\"copy_npTP\",\"likeIconWrapper\":\"likeIconWrapper_KHFx\",\"likeCount\":\"likeCount_b04X\",\"replyIconWrapper\":\"replyIconWrapper_jCup\",\"copyIconWrapper\":\"copyIconWrapper_oEVy\",\"likeIcon\":\"likeIcon_gbwt\",\"replyIcon\":\"replyIcon_ipa5\",\"copyIcon\":\"copyIcon_IhOq\",\"replyText\":\"replyText_jXTI\",\"copyText\":\"copyText_vRl0\"}";
        // 1788591701100
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 966872
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_z3Ec"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_z3Ec\"}";
        // 1788591701098
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 467581
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_D3qd","article":"article_kRZ8"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_D3qd\",\"article\":\"article_kRZ8\"}";
        // 1788591701168
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 329621
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"header":"header_bjW0","avatar":"avatar_OLqR","avatarOverflow":"avatarOverflow_eJmC","avatarSquare":"avatarSquare_iXbn","avatarShadow":"avatarShadow_wzyg","author":"author_QZ2S","authorLink":"authorLink_cyQp","authorVerified":"authorVerified_rVnZ","authorLinkText":"authorLinkText_teDJ","authorMeta":"authorMeta_qxsC","authorFollow":"authorFollow_fxrM","username":"username_qz1H","follow":"follow_seIc","separator":"separator_rJ9Q","brand":"brand_iwzK","twitterIcon":"twitterIcon__mAg"});
    if(true) {
      (function() {
        var localsJsonString = "{\"header\":\"header_bjW0\",\"avatar\":\"avatar_OLqR\",\"avatarOverflow\":\"avatarOverflow_eJmC\",\"avatarSquare\":\"avatarSquare_iXbn\",\"avatarShadow\":\"avatarShadow_wzyg\",\"author\":\"author_QZ2S\",\"authorLink\":\"authorLink_cyQp\",\"authorVerified\":\"authorVerified_rVnZ\",\"authorLinkText\":\"authorLinkText_teDJ\",\"authorMeta\":\"authorMeta_qxsC\",\"authorFollow\":\"authorFollow_fxrM\",\"username\":\"username_qz1H\",\"follow\":\"follow_seIc\",\"separator\":\"separator_rJ9Q\",\"brand\":\"brand_iwzK\",\"twitterIcon\":\"twitterIcon__mAg\"}";
        // 1788591701175
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 909042
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_iMnh"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_iMnh\"}";
        // 1788591701178
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 250863
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_FOba"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_FOba\"}";
        // 1788591701177
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 606712
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"info":"info_Kyz8","infoLink":"infoLink_coB3","infoIcon":"infoIcon_vXUt"});
    if(true) {
      (function() {
        var localsJsonString = "{\"info\":\"info_Kyz8\",\"infoLink\":\"infoLink_coB3\",\"infoIcon\":\"infoIcon_vXUt\"}";
        // 1788591701182
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 472204
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_zgtB"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_zgtB\"}";
        // 1788591701179
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 276436
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"anchor":"anchor_O_ID","videoButton":"videoButton_zM5T","videoButtonIcon":"videoButtonIcon_ShP3","watchOnTwitter":"watchOnTwitter_al_H","viewReplies":"viewReplies_OPYE"});
    if(true) {
      (function() {
        var localsJsonString = "{\"anchor\":\"anchor_O_ID\",\"videoButton\":\"videoButton_zM5T\",\"videoButtonIcon\":\"videoButtonIcon_ShP3\",\"watchOnTwitter\":\"watchOnTwitter_al_H\",\"viewReplies\":\"viewReplies_OPYE\"}";
        // 1788591701208
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 243974
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root__sIp","rounded":"rounded_dKug","mediaWrapper":"mediaWrapper_T0we","grid2Columns":"grid2Columns_YryS","grid3":"grid3_GBAC","grid2x2":"grid2x2__RM2","mediaContainer":"mediaContainer_pdV5","mediaLink":"mediaLink_tILL","skeleton":"skeleton_kXyX","image":"image_kWlZ"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root__sIp\",\"rounded\":\"rounded_dKug\",\"mediaWrapper\":\"mediaWrapper_T0we\",\"grid2Columns\":\"grid2Columns_YryS\",\"grid3\":\"grid3_GBAC\",\"grid2x2\":\"grid2x2__RM2\",\"mediaContainer\":\"mediaContainer_pdV5\",\"mediaLink\":\"mediaLink_tILL\",\"skeleton\":\"skeleton_kXyX\",\"image\":\"image_kWlZ\"}";
        // 1788591701181
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 207330
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_tqpQ"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_tqpQ\"}";
        // 1788591701210
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 822898
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"replies":"replies_enWO","link":"link_cCIX","text":"text_Zp5K"});
    if(true) {
      (function() {
        var localsJsonString = "{\"replies\":\"replies_enWO\",\"link\":\"link_cCIX\",\"text\":\"text_Zp5K\"}";
        // 1788591701209
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 506947
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"root":"root_Y6tr"});
    if(true) {
      (function() {
        var localsJsonString = "{\"root\":\"root_Y6tr\"}";
        // 1788591701212
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 307894
(module, __webpack_exports__, __webpack_require__) {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   A: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
// extracted by mini-css-extract-plugin
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ({"verifiedOld":"verifiedOld_dr_7","verifiedBlue":"verifiedBlue_d5Yb","verifiedGovernment":"verifiedGovernment_REEv"});
    if(true) {
      (function() {
        var localsJsonString = "{\"verifiedOld\":\"verifiedOld_dr_7\",\"verifiedBlue\":\"verifiedBlue_d5Yb\",\"verifiedGovernment\":\"verifiedGovernment_REEv\"}";
        // 1788591701569
        var cssReload = __webpack_require__(129140)(module.id, {"esModule":true});
        // only invalidate when locals change
        if (
          module.hot.data &&
          module.hot.data.value &&
          module.hot.data.value !== localsJsonString
        ) {
          module.hot.invalidate();
        } else {
          module.hot.accept();
        }
        module.hot.dispose(function(data) {
          data.value = localsJsonString;
          cssReload();
        });
      })();
    }
  

/***/ },

/***/ 758493
(__unused_webpack_module, exports, __webpack_require__) {

/**
 * @license React
 * use-sync-external-store-shim.production.js
 *
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */


var React = __webpack_require__(296540);
function is(x, y) {
  return (x === y && (0 !== x || 1 / x === 1 / y)) || (x !== x && y !== y);
}
var objectIs = "function" === typeof Object.is ? Object.is : is,
  useState = React.useState,
  useEffect = React.useEffect,
  useLayoutEffect = React.useLayoutEffect,
  useDebugValue = React.useDebugValue;
function useSyncExternalStore$2(subscribe, getSnapshot) {
  var value = getSnapshot(),
    _useState = useState({ inst: { value: value, getSnapshot: getSnapshot } }),
    inst = _useState[0].inst,
    forceUpdate = _useState[1];
  useLayoutEffect(
    function () {
      inst.value = value;
      inst.getSnapshot = getSnapshot;
      checkIfSnapshotChanged(inst) && forceUpdate({ inst: inst });
    },
    [subscribe, value, getSnapshot]
  );
  useEffect(
    function () {
      checkIfSnapshotChanged(inst) && forceUpdate({ inst: inst });
      return subscribe(function () {
        checkIfSnapshotChanged(inst) && forceUpdate({ inst: inst });
      });
    },
    [subscribe]
  );
  useDebugValue(value);
  return value;
}
function checkIfSnapshotChanged(inst) {
  var latestGetSnapshot = inst.getSnapshot;
  inst = inst.value;
  try {
    var nextValue = latestGetSnapshot();
    return !objectIs(inst, nextValue);
  } catch (error) {
    return !0;
  }
}
function useSyncExternalStore$1(subscribe, getSnapshot) {
  return getSnapshot();
}
var shim =
  "undefined" === typeof window ||
  "undefined" === typeof window.document ||
  "undefined" === typeof window.document.createElement
    ? useSyncExternalStore$1
    : useSyncExternalStore$2;
exports.useSyncExternalStore =
  void 0 !== React.useSyncExternalStore ? React.useSyncExternalStore : shim;


/***/ },

/***/ 19888
(module, __unused_webpack_exports, __webpack_require__) {



if (true) {
  module.exports = __webpack_require__(758493);
} else // removed by dead control flow
{}


/***/ },

/***/ 274609
(__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) {


// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  Y: () => (/* binding */ Tweet)
});

// EXTERNAL MODULE: ./node_modules/react/jsx-runtime.js
var jsx_runtime = __webpack_require__(474848);
;// ./node_modules/react-tweet/node_modules/clsx/dist/clsx.mjs
function r(e){var t,f,n="";if("string"==typeof e||"number"==typeof e)n+=e;else if("object"==typeof e)if(Array.isArray(e)){var o=e.length;for(t=0;t<o;t++)e[t]&&(f=r(e[t]))&&(n&&(n+=" "),n+=f)}else for(f in e)e[f]&&(n&&(n+=" "),n+=f);return n}function clsx(){for(var e,t,f=0,n="",o=arguments.length;f<o;f++)(e=arguments[f])&&(t=r(e))&&(n&&(n+=" "),n+=t);return n}/* harmony default export */ const dist_clsx = (clsx);
// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-container.module.css
var tweet_container_module = __webpack_require__(467581);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-container.js




const TweetContainer = ({ className, children })=>/*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
        className: dist_clsx('react-tweet-theme', tweet_container_module/* default */.A.root, className),
        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("article", {
            className: tweet_container_module/* default */.A.article,
            children: children
        })
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/skeleton.module.css
var skeleton_module = __webpack_require__(492595);
;// ./node_modules/react-tweet/dist/twitter-theme/skeleton.js


const Skeleton = ({ style })=>/*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
        className: skeleton_module/* default */.A.skeleton,
        style: style
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-skeleton.module.css
var tweet_skeleton_module = __webpack_require__(506947);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-skeleton.js




const TweetSkeleton = ()=>/*#__PURE__*/ (0,jsx_runtime.jsxs)(TweetContainer, {
        className: tweet_skeleton_module/* default */.A.root,
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsx)(Skeleton, {
                style: {
                    height: '3rem',
                    marginBottom: '0.75rem'
                }
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(Skeleton, {
                style: {
                    height: '6rem',
                    margin: '0.5rem 0'
                }
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                style: {
                    borderTop: 'var(--tweet-border)',
                    margin: '0.5rem 0'
                }
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(Skeleton, {
                style: {
                    height: '2rem'
                }
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(Skeleton, {
                style: {
                    height: '2rem',
                    borderRadius: '9999px',
                    marginTop: '0.5rem'
                }
            })
        ]
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-not-found.module.css
var tweet_not_found_module = __webpack_require__(207330);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-not-found.js



const TweetNotFound = (_props)=>/*#__PURE__*/ (0,jsx_runtime.jsx)(TweetContainer, {
        children: /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
            className: tweet_not_found_module/* default */.A.root,
            children: [
                /*#__PURE__*/ (0,jsx_runtime.jsx)("h3", {
                    children: "Tweet not found"
                }),
                /*#__PURE__*/ (0,jsx_runtime.jsx)("p", {
                    children: "The embedded tweet could not be found…"
                })
            ]
        })
    });

;// ./node_modules/react-tweet/dist/twitter-theme/avatar-img.js

// eslint-disable-next-line jsx-a11y/alt-text -- The alt text is part of `...props`
const AvatarImg = (props)=>/*#__PURE__*/ (0,jsx_runtime.jsx)("img", {
        ...props
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/icons/icons.module.css
var icons_module = __webpack_require__(614649);
;// ./node_modules/react-tweet/dist/twitter-theme/icons/verified.js


const Verified = ()=>/*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
        viewBox: "0 0 24 24",
        "aria-label": "Verified account",
        role: "img",
        className: icons_module/* default */.A.verified,
        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
            children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                d: "M22.25 12c0-1.43-.88-2.67-2.19-3.34.46-1.39.2-2.9-.81-3.91s-2.52-1.27-3.91-.81c-.66-1.31-1.91-2.19-3.34-2.19s-2.67.88-3.33 2.19c-1.4-.46-2.91-.2-3.92.81s-1.26 2.52-.8 3.91c-1.31.67-2.2 1.91-2.2 3.34s.89 2.67 2.2 3.34c-.46 1.39-.21 2.9.8 3.91s2.52 1.26 3.91.81c.67 1.31 1.91 2.19 3.34 2.19s2.68-.88 3.34-2.19c1.39.45 2.9.2 3.91-.81s1.27-2.52.81-3.91c1.31-.67 2.19-1.91 2.19-3.34zm-11.71 4.2L6.8 12.46l1.41-1.42 2.26 2.26 4.8-5.23 1.47 1.36-6.2 6.77z"
            })
        })
    });

;// ./node_modules/react-tweet/dist/twitter-theme/icons/verified-government.js


const VerifiedGovernment = ()=>/*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
        viewBox: "0 0 22 22",
        "aria-label": "Verified account",
        role: "img",
        className: icons_module/* default */.A.verified,
        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
            children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                clipRule: "evenodd",
                d: "M12.05 2.056c-.568-.608-1.532-.608-2.1 0l-1.393 1.49c-.284.303-.685.47-1.1.455L5.42 3.932c-.832-.028-1.514.654-1.486 1.486l.069 2.039c.014.415-.152.816-.456 1.1l-1.49 1.392c-.608.568-.608 1.533 0 2.101l1.49 1.393c.304.284.47.684.456 1.1l-.07 2.038c-.027.832.655 1.514 1.487 1.486l2.038-.069c.415-.014.816.152 1.1.455l1.392 1.49c.569.609 1.533.609 2.102 0l1.393-1.49c.283-.303.684-.47 1.099-.455l2.038.069c.832.028 1.515-.654 1.486-1.486L18 14.542c-.015-.415.152-.815.455-1.099l1.49-1.393c.608-.568.608-1.533 0-2.101l-1.49-1.393c-.303-.283-.47-.684-.455-1.1l.068-2.038c.029-.832-.654-1.514-1.486-1.486l-2.038.07c-.415.013-.816-.153-1.1-.456zm-5.817 9.367l3.429 3.428 5.683-6.206-1.347-1.247-4.4 4.795-2.072-2.072z",
                fillRule: "evenodd"
            })
        })
    });

;// ./node_modules/react-tweet/dist/twitter-theme/icons/verified-business.js


const VerifiedBusiness = ()=>/*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
        viewBox: "0 0 22 22",
        "aria-label": "Verified account",
        role: "img",
        className: icons_module/* default */.A.verified,
        children: /*#__PURE__*/ (0,jsx_runtime.jsxs)("g", {
            children: [
                /*#__PURE__*/ (0,jsx_runtime.jsxs)("linearGradient", {
                    gradientUnits: "userSpaceOnUse",
                    id: "0-a",
                    x1: "4.411",
                    x2: "18.083",
                    y1: "2.495",
                    y2: "21.508",
                    children: [
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("stop", {
                            offset: "0",
                            stopColor: "#f4e72a"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("stop", {
                            offset: ".539",
                            stopColor: "#cd8105"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("stop", {
                            offset: ".68",
                            stopColor: "#cb7b00"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("stop", {
                            offset: "1",
                            stopColor: "#f4ec26"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("stop", {
                            offset: "1",
                            stopColor: "#f4e72a"
                        })
                    ]
                }),
                /*#__PURE__*/ (0,jsx_runtime.jsxs)("linearGradient", {
                    gradientUnits: "userSpaceOnUse",
                    id: "0-b",
                    x1: "5.355",
                    x2: "16.361",
                    y1: "3.395",
                    y2: "19.133",
                    children: [
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("stop", {
                            offset: "0",
                            stopColor: "#f9e87f"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("stop", {
                            offset: ".406",
                            stopColor: "#e2b719"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("stop", {
                            offset: ".989",
                            stopColor: "#e2b719"
                        })
                    ]
                }),
                /*#__PURE__*/ (0,jsx_runtime.jsxs)("g", {
                    clipRule: "evenodd",
                    fillRule: "evenodd",
                    children: [
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                            d: "M13.324 3.848L11 1.6 8.676 3.848l-3.201-.453-.559 3.184L2.06 8.095 3.48 11l-1.42 2.904 2.856 1.516.559 3.184 3.201-.452L11 20.4l2.324-2.248 3.201.452.559-3.184 2.856-1.516L18.52 11l1.42-2.905-2.856-1.516-.559-3.184zm-7.09 7.575l3.428 3.428 5.683-6.206-1.347-1.247-4.4 4.795-2.072-2.072z",
                            fill: "url(#0-a)"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                            d: "M13.101 4.533L11 2.5 8.899 4.533l-2.895-.41-.505 2.88-2.583 1.37L4.2 11l-1.284 2.627 2.583 1.37.505 2.88 2.895-.41L11 19.5l2.101-2.033 2.895.41.505-2.88 2.583-1.37L17.8 11l1.284-2.627-2.583-1.37-.505-2.88zm-6.868 6.89l3.429 3.428 5.683-6.206-1.347-1.247-4.4 4.795-2.072-2.072z",
                            fill: "url(#0-b)"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                            d: "M6.233 11.423l3.429 3.428 5.65-6.17.038-.033-.005 1.398-5.683 6.206-3.429-3.429-.003-1.405.005.003z",
                            fill: "#d18800"
                        })
                    ]
                })
            ]
        })
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/verified-badge.module.css
var verified_badge_module = __webpack_require__(307894);
;// ./node_modules/react-tweet/dist/twitter-theme/verified-badge.js




const VerifiedBadge = ({ user, className })=>{
    const verified = user.verified || user.is_blue_verified || user.verified_type;
    let icon = /*#__PURE__*/ (0,jsx_runtime.jsx)(Verified, {});
    let iconClassName = verified_badge_module/* default */.A.verifiedBlue;
    if (verified) {
        if (!user.is_blue_verified) {
            iconClassName = verified_badge_module/* default */.A.verifiedOld;
        }
        switch(user.verified_type){
            case 'Government':
                icon = /*#__PURE__*/ (0,jsx_runtime.jsx)(VerifiedGovernment, {});
                iconClassName = verified_badge_module/* default */.A.verifiedGovernment;
                break;
            case 'Business':
                icon = /*#__PURE__*/ (0,jsx_runtime.jsx)(VerifiedBusiness, {});
                iconClassName = null;
                break;
        }
    }
    return verified ? /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
        className: dist_clsx(className, iconClassName),
        children: icon
    }) : null;
};

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/highlighted-label.module.css
var highlighted_label_module = __webpack_require__(684876);
;// ./node_modules/react-tweet/dist/twitter-theme/highlighted-label.js



const HighlightedLabel = ({ user, className })=>{
    var _label_badge;
    const label = user.highlighted_label;
    if (!label) return null;
    const url = (_label_badge = label.badge) == null ? void 0 : _label_badge.url;
    if (!url) return null;
    return /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
        className: dist_clsx(highlighted_label_module/* default */.A.label, className),
        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("img", {
            src: url,
            alt: label.description
        })
    });
};

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-header.module.css
var tweet_header_module = __webpack_require__(329621);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-header.js






const TweetHeader = ({ tweet, components })=>{
    var _ref;
    const Img = (_ref = components == null ? void 0 : components.AvatarImg) != null ? _ref : AvatarImg;
    const { user } = tweet;
    return /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
        className: tweet_header_module/* default */.A.header,
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsxs)("a", {
                href: tweet.url,
                className: tweet_header_module/* default */.A.avatar,
                target: "_blank",
                rel: "noopener noreferrer",
                children: [
                    /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                        className: dist_clsx(tweet_header_module/* default */.A.avatarOverflow, user.profile_image_shape === 'Square' && tweet_header_module/* default */.A.avatarSquare),
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)(Img, {
                            src: user.profile_image_url_https,
                            alt: user.name,
                            width: 48,
                            height: 48
                        })
                    }),
                    /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                        className: tweet_header_module/* default */.A.avatarOverflow,
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                            className: tweet_header_module/* default */.A.avatarShadow
                        })
                    })
                ]
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
                className: tweet_header_module/* default */.A.author,
                children: [
                    /*#__PURE__*/ (0,jsx_runtime.jsxs)("a", {
                        href: tweet.url,
                        className: tweet_header_module/* default */.A.authorLink,
                        target: "_blank",
                        rel: "noopener noreferrer",
                        children: [
                            /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                                className: tweet_header_module/* default */.A.authorLinkText,
                                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                                    title: user.name,
                                    children: user.name
                                })
                            }),
                            /*#__PURE__*/ (0,jsx_runtime.jsx)(VerifiedBadge, {
                                user: user,
                                className: tweet_header_module/* default */.A.authorVerified
                            }),
                            /*#__PURE__*/ (0,jsx_runtime.jsx)(HighlightedLabel, {
                                user: user
                            })
                        ]
                    }),
                    /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
                        className: tweet_header_module/* default */.A.authorMeta,
                        children: [
                            /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
                                href: tweet.url,
                                className: tweet_header_module/* default */.A.username,
                                target: "_blank",
                                rel: "noopener noreferrer",
                                children: /*#__PURE__*/ (0,jsx_runtime.jsxs)("span", {
                                    title: `@${user.screen_name}`,
                                    children: [
                                        "@",
                                        user.screen_name
                                    ]
                                })
                            }),
                            /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
                                className: tweet_header_module/* default */.A.authorFollow,
                                children: [
                                    /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                                        className: tweet_header_module/* default */.A.separator,
                                        children: "·"
                                    }),
                                    /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
                                        href: user.follow_url,
                                        className: tweet_header_module/* default */.A.follow,
                                        target: "_blank",
                                        rel: "noopener noreferrer",
                                        children: "Follow"
                                    })
                                ]
                            })
                        ]
                    })
                ]
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
                href: tweet.url,
                className: tweet_header_module/* default */.A.brand,
                target: "_blank",
                rel: "noopener noreferrer",
                "aria-label": "View on Twitter",
                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
                    viewBox: "0 0 24 24",
                    "aria-hidden": "true",
                    className: tweet_header_module/* default */.A.twitterIcon,
                    children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                            d: "M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"
                        })
                    })
                })
            })
        ]
    });
};

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-in-reply-to.module.css
var tweet_in_reply_to_module = __webpack_require__(909042);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-in-reply-to.js


const TweetInReplyTo = ({ tweet })=>/*#__PURE__*/ (0,jsx_runtime.jsxs)("a", {
        href: tweet.in_reply_to_url,
        className: tweet_in_reply_to_module/* default */.A.root,
        target: "_blank",
        rel: "noopener noreferrer",
        children: [
            "Replying to @",
            tweet.in_reply_to_screen_name
        ]
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-link.module.css
var tweet_link_module = __webpack_require__(472204);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-link.js


const TweetLink = ({ href, children })=>/*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
        href: href,
        className: tweet_link_module/* default */.A.root,
        target: "_blank",
        rel: "noopener noreferrer nofollow",
        children: children
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-body.module.css
var tweet_body_module = __webpack_require__(966872);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-body.js



const TweetBody = ({ tweet })=>/*#__PURE__*/ (0,jsx_runtime.jsxs)("p", {
        className: tweet_body_module/* default */.A.root,
        lang: tweet.lang,
        dir: "auto",
        children: [
            tweet.entities.map((item, i)=>{
                switch(item.type){
                    case 'hashtag':
                    case 'mention':
                    case 'url':
                    case 'symbol':
                        return /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetLink, {
                            href: item.href,
                            children: item.text
                        }, i);
                    case 'media':
                        // Media text is currently never displayed, some tweets however might have indices
                        // that do match `display_text_range` so for those cases we ignore the content.
                        return;
                    default:
                        // We use `dangerouslySetInnerHTML` to preserve the text encoding.
                        // https://github.com/vercel/react-tweet/issues/29
                        return /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                            dangerouslySetInnerHTML: {
                                __html: item.text
                            }
                        }, i);
                }
            }),
            tweet.note_tweet ? /*#__PURE__*/ (0,jsx_runtime.jsx)(ShowMore, {
                tweet: tweet
            }) : null
        ]
    });
function ShowMore({ tweet }) {
    return /*#__PURE__*/ (0,jsx_runtime.jsxs)(TweetLink, {
        href: tweet.url,
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                children: " "
            }),
            "Show more"
        ]
    });
}

// EXTERNAL MODULE: ./node_modules/react/index.js
var react = __webpack_require__(296540);
;// ./node_modules/react-tweet/dist/utils.js

const getTweetUrl = (tweet)=>`https://x.com/${tweet.user.screen_name}/status/${tweet.id_str}`;
const getUserUrl = (usernameOrTweet)=>`https://x.com/${typeof usernameOrTweet === 'string' ? usernameOrTweet : usernameOrTweet.user.screen_name}`;
const getLikeUrl = (tweet)=>`https://x.com/intent/like?tweet_id=${tweet.id_str}`;
const getReplyUrl = (tweet)=>`https://x.com/intent/tweet?in_reply_to=${tweet.id_str}`;
const getFollowUrl = (tweet)=>`https://x.com/intent/follow?screen_name=${tweet.user.screen_name}`;
const getHashtagUrl = (hashtag)=>`https://x.com/hashtag/${hashtag.text}`;
const getSymbolUrl = (symbol)=>`https://x.com/search?q=%24${symbol.text}`;
const getInReplyToUrl = (tweet)=>`https://x.com/${tweet.in_reply_to_screen_name}/status/${tweet.in_reply_to_status_id_str}`;
const getMediaUrl = (media, size)=>{
    const url = new URL(media.media_url_https);
    const extension = url.pathname.split('.').pop();
    if (!extension) return media.media_url_https;
    url.pathname = url.pathname.replace(`.${extension}`, '');
    url.searchParams.set('format', extension);
    url.searchParams.set('name', size);
    return url.toString();
};
const getMp4Videos = (media)=>{
    const { variants } = media.video_info;
    const sortedMp4Videos = variants.filter((vid)=>vid.content_type === 'video/mp4').sort((a, b)=>{
        var _b_bitrate, _a_bitrate;
        return ((_b_bitrate = b.bitrate) != null ? _b_bitrate : 0) - ((_a_bitrate = a.bitrate) != null ? _a_bitrate : 0);
    });
    return sortedMp4Videos;
};
const getMp4Video = (media)=>{
    const mp4Videos = getMp4Videos(media);
    // Skip the highest quality video and use the next quality
    return mp4Videos.length > 1 ? mp4Videos[1] : mp4Videos[0];
};
const formatNumber = (n)=>{
    if (n > 999999) return `${(n / 1000000).toFixed(1)}M`;
    if (n > 999) return `${(n / 1000).toFixed(1)}K`;
    return n.toString();
};
function getEntities(tweet) {
    const textMap = Array.from(tweet.text);
    const result = [
        {
            indices: tweet.display_text_range,
            type: 'text'
        }
    ];
    const { entities } = tweet;
    if (entities) {
        addEntities(result, 'hashtag', entities.hashtags);
        addEntities(result, 'mention', entities.user_mentions);
        addEntities(result, 'url', entities.urls);
        addEntities(result, 'symbol', entities.symbols);
        addEntities(result, 'media', entities.media);
    }
    fixRange(tweet, result);
    return result.map((entity)=>{
        const text = textMap.slice(entity.indices[0], entity.indices[1]).join('');
        switch(entity.type){
            case 'hashtag':
                return Object.assign(entity, {
                    href: getHashtagUrl(entity),
                    text
                });
            case 'mention':
                return Object.assign(entity, {
                    href: getUserUrl(entity.screen_name),
                    text
                });
            case 'url':
            case 'media':
                return Object.assign(entity, {
                    href: entity.expanded_url,
                    text: entity.display_url
                });
            case 'symbol':
                return Object.assign(entity, {
                    href: getSymbolUrl(entity),
                    text
                });
            default:
                return Object.assign(entity, {
                    text
                });
        }
    });
}
function addEntities(result, type, entities) {
    if (!(entities == null ? void 0 : entities.length)) return;
    for (const entity of entities){
        for (const [i, item] of result.entries()){
            if (item.indices[0] > entity.indices[0] || item.indices[1] < entity.indices[1]) {
                continue;
            }
            const items = [
                {
                    ...entity,
                    type
                }
            ];
            if (item.indices[0] < entity.indices[0]) {
                items.unshift({
                    indices: [
                        item.indices[0],
                        entity.indices[0]
                    ],
                    type: 'text'
                });
            }
            if (item.indices[1] > entity.indices[1]) {
                items.push({
                    indices: [
                        entity.indices[1],
                        item.indices[1]
                    ],
                    type: 'text'
                });
            }
            result.splice(i, 1, ...items);
            break; // Break out of the loop to avoid iterating over the new items
        }
    }
}
/**
 * Update display_text_range to work w/ Array.from
 * Array.from is unicode aware, unlike string.slice()
 */ function fixRange(tweet, entities) {
    var _tweet_entities;
    const media = (_tweet_entities = tweet.entities) == null ? void 0 : _tweet_entities.media;
    if ((media == null ? void 0 : media.length) && media[0].indices[0] < tweet.display_text_range[1]) {
        tweet.display_text_range[1] = media[0].indices[0];
    }
    const lastEntity = entities.at(-1);
    if (lastEntity && lastEntity.indices[1] > tweet.display_text_range[1]) {
        lastEntity.indices[1] = tweet.display_text_range[1];
    }
}
/**
 * Enriches a tweet with additional data used to more easily use the tweet in a UI.
 */ const enrichTweet = (tweet)=>({
        ...tweet,
        url: getTweetUrl(tweet),
        user: {
            ...tweet.user,
            url: getUserUrl(tweet),
            follow_url: getFollowUrl(tweet)
        },
        like_url: getLikeUrl(tweet),
        reply_url: getReplyUrl(tweet),
        in_reply_to_url: tweet.in_reply_to_screen_name ? getInReplyToUrl(tweet) : undefined,
        entities: getEntities(tweet),
        quoted_tweet: tweet.quoted_tweet ? {
            ...tweet.quoted_tweet,
            url: getTweetUrl(tweet.quoted_tweet),
            entities: getEntities(tweet.quoted_tweet)
        } : undefined
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-media.module.css
var tweet_media_module = __webpack_require__(243974);
// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-media-video.module.css
var tweet_media_video_module = __webpack_require__(276436);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-media-video.js
'use client';






const TweetMediaVideo = ({ tweet, media })=>{
    const [playButton, setPlayButton] = (0,react.useState)(true);
    const [isPlaying, setIsPlaying] = (0,react.useState)(false);
    const [ended, setEnded] = (0,react.useState)(false);
    const mp4Video = getMp4Video(media);
    let timeout = 0;
    return /*#__PURE__*/ (0,jsx_runtime.jsxs)(jsx_runtime.Fragment, {
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsx)("video", {
                className: tweet_media_module/* default */.A.image,
                poster: getMediaUrl(media, 'small'),
                controls: !playButton,
                playsInline: true,
                preload: "none",
                tabIndex: playButton ? -1 : 0,
                onPlay: ()=>{
                    if (timeout) window.clearTimeout(timeout);
                    if (!isPlaying) setIsPlaying(true);
                    if (ended) setEnded(false);
                },
                onPause: ()=>{
                    // When the video is seeked (moved to a different timestamp), it will pause for a moment
                    // before resuming. We don't want to show the message in that case so we wait a bit.
                    if (timeout) window.clearTimeout(timeout);
                    timeout = window.setTimeout(()=>{
                        if (isPlaying) setIsPlaying(false);
                        timeout = 0;
                    }, 100);
                },
                onEnded: ()=>{
                    setEnded(true);
                },
                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("source", {
                    src: mp4Video.url,
                    type: mp4Video.content_type
                })
            }),
            playButton && /*#__PURE__*/ (0,jsx_runtime.jsx)("button", {
                type: "button",
                className: tweet_media_video_module/* default */.A.videoButton,
                "aria-label": "View video on X",
                onClick: (e)=>{
                    const video = e.currentTarget.previousSibling;
                    e.preventDefault();
                    setPlayButton(false);
                    video.load();
                    video.play().then(()=>{
                        setIsPlaying(true);
                        video.focus();
                    }).catch((error)=>{
                        console.error('Error playing video:', error);
                        setPlayButton(true);
                        setIsPlaying(false);
                    });
                },
                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
                    viewBox: "0 0 24 24",
                    className: tweet_media_video_module/* default */.A.videoButtonIcon,
                    "aria-hidden": "true",
                    children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                            d: "M21 12L4 2v20l17-10z"
                        })
                    })
                })
            }),
            !isPlaying && !ended && /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                className: tweet_media_video_module/* default */.A.watchOnTwitter,
                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
                    href: tweet.url,
                    className: tweet_media_video_module/* default */.A.anchor,
                    target: "_blank",
                    rel: "noopener noreferrer",
                    children: playButton ? 'Watch on X' : 'Continue watching on X'
                })
            }),
            ended && /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
                href: tweet.url,
                className: dist_clsx(tweet_media_video_module/* default */.A.anchor, tweet_media_video_module/* default */.A.viewReplies),
                target: "_blank",
                rel: "noopener noreferrer",
                children: "View replies"
            })
        ]
    });
};

;// ./node_modules/react-tweet/dist/twitter-theme/media-img.js

// eslint-disable-next-line jsx-a11y/alt-text -- The alt text is part of `...props`
const MediaImg = (props)=>/*#__PURE__*/ (0,jsx_runtime.jsx)("img", {
        ...props
    });

;// ./node_modules/react-tweet/dist/twitter-theme/tweet-media.js







const getSkeletonStyle = (media, itemCount)=>{
    let paddingBottom = 56.25 // default of 16x9
    ;
    // if we only have 1 item, show at original ratio
    if (itemCount === 1) paddingBottom = 100 / media.original_info.width * media.original_info.height;
    // if we have 2 items, double the default to be 16x9 total
    if (itemCount === 2) paddingBottom = paddingBottom * 2;
    return {
        width: media.type === 'photo' ? undefined : 'unset',
        paddingBottom: `${paddingBottom}%`
    };
};
const TweetMedia = ({ tweet, components, quoted })=>{
    var _ref, _ref1;
    var _tweet_mediaDetails, _tweet_mediaDetails1;
    const length = (_ref = (_tweet_mediaDetails = tweet.mediaDetails) == null ? void 0 : _tweet_mediaDetails.length) != null ? _ref : 0;
    const Img = (_ref1 = components == null ? void 0 : components.MediaImg) != null ? _ref1 : MediaImg;
    return /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
        className: dist_clsx(tweet_media_module/* default */.A.root, !quoted && tweet_media_module/* default */.A.rounded),
        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
            className: dist_clsx(tweet_media_module/* default */.A.mediaWrapper, length > 1 && tweet_media_module/* default */.A.grid2Columns, length === 3 && tweet_media_module/* default */.A.grid3, length > 4 && tweet_media_module/* default */.A.grid2x2),
            children: (_tweet_mediaDetails1 = tweet.mediaDetails) == null ? void 0 : _tweet_mediaDetails1.map((media)=>/*#__PURE__*/ (0,jsx_runtime.jsx)(react.Fragment, {
                    children: media.type === 'photo' ? /*#__PURE__*/ (0,jsx_runtime.jsxs)("a", {
                        href: tweet.url,
                        className: dist_clsx(tweet_media_module/* default */.A.mediaContainer, tweet_media_module/* default */.A.mediaLink),
                        target: "_blank",
                        rel: "noopener noreferrer",
                        children: [
                            /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                                className: tweet_media_module/* default */.A.skeleton,
                                style: getSkeletonStyle(media, length)
                            }),
                            /*#__PURE__*/ (0,jsx_runtime.jsx)(Img, {
                                src: getMediaUrl(media, 'small'),
                                alt: media.ext_alt_text || 'Image',
                                className: tweet_media_module/* default */.A.image,
                                draggable: true
                            })
                        ]
                    }, media.media_url_https) : /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
                        className: tweet_media_module/* default */.A.mediaContainer,
                        children: [
                            /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                                className: tweet_media_module/* default */.A.skeleton,
                                style: getSkeletonStyle(media, length)
                            }),
                            /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetMediaVideo, {
                                tweet: tweet,
                                media: media
                            })
                        ]
                    }, media.media_url_https)
                }, media.media_url_https))
        })
    });
};

;// ./node_modules/react-tweet/dist/date-utils.js
const options = {
    hour: 'numeric',
    minute: '2-digit',
    hour12: true,
    weekday: 'short',
    month: 'short',
    day: 'numeric',
    year: 'numeric'
};
const formatter = new Intl.DateTimeFormat('en-US', options);
const partsArrayToObject = (parts)=>{
    const result = {};
    for (const part of parts){
        result[part.type] = part.value;
    }
    return result;
};
const formatDate = (date)=>{
    const parts = partsArrayToObject(formatter.formatToParts(date));
    const formattedTime = `${parts.hour}:${parts.minute} ${parts.dayPeriod}`;
    const formattedDate = `${parts.month} ${parts.day}, ${parts.year}`;
    return `${formattedTime} · ${formattedDate}`;
};

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-info-created-at.module.css
var tweet_info_created_at_module = __webpack_require__(250863);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-info-created-at.js



const TweetInfoCreatedAt = ({ tweet })=>{
    const createdAt = new Date(tweet.created_at);
    const formattedCreatedAtDate = formatDate(createdAt);
    return /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
        className: tweet_info_created_at_module/* default */.A.root,
        href: tweet.url,
        target: "_blank",
        rel: "noopener noreferrer",
        "aria-label": formattedCreatedAtDate,
        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("time", {
            dateTime: createdAt.toISOString(),
            children: formattedCreatedAtDate
        })
    });
};

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-info.module.css
var tweet_info_module = __webpack_require__(606712);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-info.js



const TweetInfo = ({ tweet })=>/*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
        className: tweet_info_module/* default */.A.info,
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetInfoCreatedAt, {
                tweet: tweet
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
                className: tweet_info_module/* default */.A.infoLink,
                href: "https://help.x.com/en/x-for-websites-ads-info-and-privacy",
                target: "_blank",
                rel: "noopener noreferrer",
                "aria-label": "Twitter for Websites, Ads Information and Privacy",
                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
                    viewBox: "0 0 24 24",
                    "aria-hidden": "true",
                    className: tweet_info_module/* default */.A.infoIcon,
                    children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                            d: "M13.5 8.5c0 .83-.67 1.5-1.5 1.5s-1.5-.67-1.5-1.5S11.17 7 12 7s1.5.67 1.5 1.5zM13 17v-5h-2v5h2zm-1 5.25c5.66 0 10.25-4.59 10.25-10.25S17.66 1.75 12 1.75 1.75 6.34 1.75 12 6.34 22.25 12 22.25zM20.25 12c0 4.56-3.69 8.25-8.25 8.25S3.75 16.56 3.75 12 7.44 3.75 12 3.75s8.25 3.69 8.25 8.25z"
                        })
                    })
                })
            })
        ]
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-actions.module.css
var tweet_actions_module = __webpack_require__(886649);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-actions-copy.js
'use client';



const TweetActionsCopy = ({ tweet })=>{
    const [copied, setCopied] = (0,react.useState)(false);
    const handleCopy = ()=>{
        navigator.clipboard.writeText(tweet.url);
        setCopied(true);
    };
    (0,react.useEffect)(()=>{
        if (copied) {
            const timeout = setTimeout(()=>{
                setCopied(false);
            }, 6000);
            return ()=>clearTimeout(timeout);
        }
    }, [
        copied
    ]);
    return /*#__PURE__*/ (0,jsx_runtime.jsxs)("button", {
        type: "button",
        className: tweet_actions_module/* default */.A.copy,
        "aria-label": "Copy link",
        onClick: handleCopy,
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                className: tweet_actions_module/* default */.A.copyIconWrapper,
                children: copied ? /*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
                    viewBox: "0 0 24 24",
                    className: tweet_actions_module/* default */.A.copyIcon,
                    "aria-hidden": "true",
                    children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                            d: "M9.64 18.952l-5.55-4.861 1.317-1.504 3.951 3.459 8.459-10.948L19.4 6.32 9.64 18.952z"
                        })
                    })
                }) : /*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
                    viewBox: "0 0 24 24",
                    className: tweet_actions_module/* default */.A.copyIcon,
                    "aria-hidden": "true",
                    children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                            d: "M18.36 5.64c-1.95-1.96-5.11-1.96-7.07 0L9.88 7.05 8.46 5.64l1.42-1.42c2.73-2.73 7.16-2.73 9.9 0 2.73 2.74 2.73 7.17 0 9.9l-1.42 1.42-1.41-1.42 1.41-1.41c1.96-1.96 1.96-5.12 0-7.07zm-2.12 3.53l-7.07 7.07-1.41-1.41 7.07-7.07 1.41 1.41zm-12.02.71l1.42-1.42 1.41 1.42-1.41 1.41c-1.96 1.96-1.96 5.12 0 7.07 1.95 1.96 5.11 1.96 7.07 0l1.41-1.41 1.42 1.41-1.42 1.42c-2.73 2.73-7.16 2.73-9.9 0-2.73-2.74-2.73-7.17 0-9.9z"
                        })
                    })
                })
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                className: tweet_actions_module/* default */.A.copyText,
                children: copied ? 'Copied!' : 'Copy link'
            })
        ]
    });
};

;// ./node_modules/react-tweet/dist/twitter-theme/tweet-actions.js




const TweetActions = ({ tweet })=>{
    const favoriteCount = formatNumber(tweet.favorite_count);
    return /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
        className: tweet_actions_module/* default */.A.actions,
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsxs)("a", {
                className: tweet_actions_module/* default */.A.like,
                href: tweet.like_url,
                target: "_blank",
                rel: "noopener noreferrer",
                "aria-label": `Like. This Tweet has ${favoriteCount} likes`,
                children: [
                    /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                        className: tweet_actions_module/* default */.A.likeIconWrapper,
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
                            viewBox: "0 0 24 24",
                            className: tweet_actions_module/* default */.A.likeIcon,
                            "aria-hidden": "true",
                            children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
                                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                                    d: "M20.884 13.19c-1.351 2.48-4.001 5.12-8.379 7.67l-.503.3-.504-.3c-4.379-2.55-7.029-5.19-8.382-7.67-1.36-2.5-1.41-4.86-.514-6.67.887-1.79 2.647-2.91 4.601-3.01 1.651-.09 3.368.56 4.798 2.01 1.429-1.45 3.146-2.1 4.796-2.01 1.954.1 3.714 1.22 4.601 3.01.896 1.81.846 4.17-.514 6.67z"
                                })
                            })
                        })
                    }),
                    /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                        className: tweet_actions_module/* default */.A.likeCount,
                        children: favoriteCount
                    })
                ]
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsxs)("a", {
                className: tweet_actions_module/* default */.A.reply,
                href: tweet.reply_url,
                target: "_blank",
                rel: "noopener noreferrer",
                "aria-label": "Reply to this Tweet on Twitter",
                children: [
                    /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                        className: tweet_actions_module/* default */.A.replyIconWrapper,
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("svg", {
                            viewBox: "0 0 24 24",
                            className: tweet_actions_module/* default */.A.replyIcon,
                            "aria-hidden": "true",
                            children: /*#__PURE__*/ (0,jsx_runtime.jsx)("g", {
                                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("path", {
                                    d: "M1.751 10c0-4.42 3.584-8 8.005-8h4.366c4.49 0 8.129 3.64 8.129 8.13 0 2.96-1.607 5.68-4.196 7.11l-8.054 4.46v-3.69h-.067c-4.49.1-8.183-3.51-8.183-8.01z"
                                })
                            })
                        })
                    }),
                    /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                        className: tweet_actions_module/* default */.A.replyText,
                        children: "Reply"
                    })
                ]
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetActionsCopy, {
                tweet: tweet
            })
        ]
    });
};

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/tweet-replies.module.css
var tweet_replies_module = __webpack_require__(822898);
;// ./node_modules/react-tweet/dist/twitter-theme/tweet-replies.js



const TweetReplies = ({ tweet })=>/*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
        className: tweet_replies_module/* default */.A.replies,
        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
            className: tweet_replies_module/* default */.A.link,
            href: tweet.url,
            target: "_blank",
            rel: "noopener noreferrer",
            children: /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                className: tweet_replies_module/* default */.A.text,
                children: tweet.conversation_count === 0 ? 'Read more on X' : tweet.conversation_count === 1 ? `Read ${formatNumber(tweet.conversation_count)} reply` : `Read ${formatNumber(tweet.conversation_count)} replies`
            })
        })
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/quoted-tweet/quoted-tweet-container.module.css
var quoted_tweet_container_module = __webpack_require__(495489);
;// ./node_modules/react-tweet/dist/twitter-theme/quoted-tweet/quoted-tweet-container.js
'use client';


const QuotedTweetContainer = ({ tweet, children })=>/*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
        className: quoted_tweet_container_module/* default */.A.root,
        onClick: (e)=>{
            e.preventDefault();
            window.open(tweet.url, '_blank');
        },
        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("article", {
            className: quoted_tweet_container_module/* default */.A.article,
            children: children
        })
    });

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/quoted-tweet/quoted-tweet-header.module.css
var quoted_tweet_header_module = __webpack_require__(592289);
;// ./node_modules/react-tweet/dist/twitter-theme/quoted-tweet/quoted-tweet-header.js






const QuotedTweetHeader = ({ tweet, components })=>{
    var _ref;
    const { user } = tweet;
    const Img = (_ref = components == null ? void 0 : components.AvatarImg) != null ? _ref : AvatarImg;
    return /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
        className: quoted_tweet_header_module/* default */.A.header,
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsx)("a", {
                href: tweet.url,
                className: quoted_tweet_header_module/* default */.A.avatar,
                target: "_blank",
                rel: "noopener noreferrer",
                children: /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                    className: dist_clsx(quoted_tweet_header_module/* default */.A.avatarOverflow, user.profile_image_shape === 'Square' && quoted_tweet_header_module/* default */.A.avatarSquare),
                    children: /*#__PURE__*/ (0,jsx_runtime.jsx)(Img, {
                        src: user.profile_image_url_https,
                        alt: user.name,
                        width: 20,
                        height: 20
                    })
                })
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsxs)("div", {
                className: quoted_tweet_header_module/* default */.A.author,
                children: [
                    /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                        className: quoted_tweet_header_module/* default */.A.authorText,
                        children: /*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                            title: user.name,
                            children: user.name
                        })
                    }),
                    /*#__PURE__*/ (0,jsx_runtime.jsx)(VerifiedBadge, {
                        user: user
                    }),
                    /*#__PURE__*/ (0,jsx_runtime.jsx)(HighlightedLabel, {
                        user: user
                    }),
                    /*#__PURE__*/ (0,jsx_runtime.jsx)("div", {
                        className: quoted_tweet_header_module/* default */.A.username,
                        children: /*#__PURE__*/ (0,jsx_runtime.jsxs)("span", {
                            title: `@${user.screen_name}`,
                            children: [
                                "@",
                                user.screen_name
                            ]
                        })
                    })
                ]
            })
        ]
    });
};

// EXTERNAL MODULE: ./node_modules/react-tweet/dist/twitter-theme/quoted-tweet/quoted-tweet-body.module.css
var quoted_tweet_body_module = __webpack_require__(871660);
;// ./node_modules/react-tweet/dist/twitter-theme/quoted-tweet/quoted-tweet-body.js


const QuotedTweetBody = ({ tweet })=>/*#__PURE__*/ (0,jsx_runtime.jsx)("p", {
        className: quoted_tweet_body_module/* default */.A.root,
        lang: tweet.lang,
        dir: "auto",
        children: tweet.entities.map((item, i)=>/*#__PURE__*/ (0,jsx_runtime.jsx)("span", {
                dangerouslySetInnerHTML: {
                    __html: item.text
                }
            }, i))
    });

;// ./node_modules/react-tweet/dist/twitter-theme/quoted-tweet/quoted-tweet.js





const QuotedTweet = ({ tweet, components })=>{
    var _tweet_mediaDetails;
    return /*#__PURE__*/ (0,jsx_runtime.jsxs)(QuotedTweetContainer, {
        tweet: tweet,
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsx)(QuotedTweetHeader, {
                tweet: tweet,
                components: components
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(QuotedTweetBody, {
                tweet: tweet
            }),
            ((_tweet_mediaDetails = tweet.mediaDetails) == null ? void 0 : _tweet_mediaDetails.length) ? /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetMedia, {
                quoted: true,
                tweet: tweet
            }) : null
        ]
    });
};

;// ./node_modules/react-tweet/dist/twitter-theme/embedded-tweet.js












const EmbeddedTweet = ({ tweet: t, components })=>{
    var _tweet_mediaDetails;
    // useMemo does nothing for RSC but it helps when the component is used in the client (e.g by SWR)
    const tweet = (0,react.useMemo)(()=>enrichTweet(t), [
        t
    ]);
    return /*#__PURE__*/ (0,jsx_runtime.jsxs)(TweetContainer, {
        children: [
            /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetHeader, {
                tweet: tweet,
                components: components
            }),
            tweet.in_reply_to_status_id_str && /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetInReplyTo, {
                tweet: tweet
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetBody, {
                tweet: tweet
            }),
            ((_tweet_mediaDetails = tweet.mediaDetails) == null ? void 0 : _tweet_mediaDetails.length) ? /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetMedia, {
                tweet: tweet,
                components: components
            }) : null,
            tweet.quoted_tweet && /*#__PURE__*/ (0,jsx_runtime.jsx)(QuotedTweet, {
                tweet: tweet.quoted_tweet,
                components: components
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetInfo, {
                tweet: tweet
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetActions, {
                tweet: tweet
            }),
            /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetReplies, {
                tweet: tweet
            })
        ]
    });
};

// EXTERNAL MODULE: ./node_modules/use-sync-external-store/shim/index.js
var shim = __webpack_require__(19888);
;// ./node_modules/swr/dist/_internal/events.mjs
const FOCUS_EVENT = 0;
const RECONNECT_EVENT = 1;
const MUTATE_EVENT = 2;
const ERROR_REVALIDATE_EVENT = 3;



;// ./node_modules/dequal/lite/index.mjs
var has = Object.prototype.hasOwnProperty;

function dequal(foo, bar) {
	var ctor, len;
	if (foo === bar) return true;

	if (foo && bar && (ctor=foo.constructor) === bar.constructor) {
		if (ctor === Date) return foo.getTime() === bar.getTime();
		if (ctor === RegExp) return foo.toString() === bar.toString();

		if (ctor === Array) {
			if ((len=foo.length) === bar.length) {
				while (len-- && dequal(foo[len], bar[len]));
			}
			return len === -1;
		}

		if (!ctor || typeof foo === 'object') {
			len = 0;
			for (ctor in foo) {
				if (has.call(foo, ctor) && ++len && !has.call(bar, ctor)) return false;
				if (!(ctor in bar) || !dequal(foo[ctor], bar[ctor])) return false;
			}
			return Object.keys(bar).length === len;
		}
	}

	return foo !== foo && bar !== bar;
}

;// ./node_modules/swr/dist/_internal/config-context-12s-fLme8m6q.mjs
'use client';




// Global state used to deduplicate requests and store listeners
const SWRGlobalState = new WeakMap();

// Shared state between server components and client components
const noop = ()=>{};
// Using noop() as the undefined value as undefined can be replaced
// by something else. Prettier ignore and extra parentheses are necessary here
// to ensure that tsc doesn't remove the __NOINLINE__ comment.
// prettier-ignore
const UNDEFINED = /*#__NOINLINE__*/ noop();
const OBJECT = Object;
const isUndefined = (v)=>v === UNDEFINED;
const isFunction = (v)=>typeof v == 'function';
const mergeObjects = (a, b)=>({
        ...a,
        ...b
    });
const isPromiseLike = (x)=>isFunction(x.then);

const EMPTY_CACHE = {};
const INITIAL_CACHE = {};
const STR_UNDEFINED = 'undefined';
// NOTE: Use the function to guarantee it's re-evaluated between jsdom and node runtime for tests.
const isWindowDefined = typeof window != STR_UNDEFINED;
const isDocumentDefined = typeof document != STR_UNDEFINED;
const isLegacyDeno = isWindowDefined && 'Deno' in window;
const hasRequestAnimationFrame = ()=>isWindowDefined && typeof window['requestAnimationFrame'] != STR_UNDEFINED;
const createCacheHelper = (cache, key)=>{
    const state = SWRGlobalState.get(cache);
    return [
        // Getter
        ()=>!isUndefined(key) && cache.get(key) || EMPTY_CACHE,
        // Setter
        (info)=>{
            if (!isUndefined(key)) {
                const prev = cache.get(key);
                // Before writing to the store, we keep the value in the initial cache
                // if it's not there yet.
                if (!(key in INITIAL_CACHE)) {
                    INITIAL_CACHE[key] = prev;
                }
                state[5](key, mergeObjects(prev, info), prev || EMPTY_CACHE);
            }
        },
        // Subscriber
        state[6],
        // Get server cache snapshot
        ()=>{
            if (!isUndefined(key)) {
                // If the cache was updated on the client, we return the stored initial value.
                if (key in INITIAL_CACHE) return INITIAL_CACHE[key];
            }
            // If we haven't done any client-side updates, we return the current value.
            return !isUndefined(key) && cache.get(key) || EMPTY_CACHE;
        }
    ];
} // export { UNDEFINED, OBJECT, isUndefined, isFunction, mergeObjects, isPromiseLike }
;

/**
 * Due to the bug https://bugs.chromium.org/p/chromium/issues/detail?id=678075,
 * it's not reliable to detect if the browser is currently online or offline
 * based on `navigator.onLine`.
 * As a workaround, we always assume it's online on the first load, and change
 * the status upon `online` or `offline` events.
 */ let online = true;
const isOnline = ()=>online;
// For node and React Native, `add/removeEventListener` doesn't exist on window.
const [onWindowEvent, offWindowEvent] = isWindowDefined && window.addEventListener ? [
    window.addEventListener.bind(window),
    window.removeEventListener.bind(window)
] : [
    noop,
    noop
];
const isVisible = ()=>{
    const visibilityState = isDocumentDefined && document.visibilityState;
    return isUndefined(visibilityState) || visibilityState !== 'hidden';
};
const initFocus = (callback)=>{
    // focus revalidate
    if (isDocumentDefined) {
        document.addEventListener('visibilitychange', callback);
    }
    onWindowEvent('focus', callback);
    return ()=>{
        if (isDocumentDefined) {
            document.removeEventListener('visibilitychange', callback);
        }
        offWindowEvent('focus', callback);
    };
};
const initReconnect = (callback)=>{
    // revalidate on reconnected
    const onOnline = ()=>{
        online = true;
        callback();
    };
    // nothing to revalidate, just update the status
    const onOffline = ()=>{
        online = false;
    };
    onWindowEvent('online', onOnline);
    onWindowEvent('offline', onOffline);
    return ()=>{
        offWindowEvent('online', onOnline);
        offWindowEvent('offline', onOffline);
    };
};
const preset = {
    isOnline,
    isVisible
};
const defaultConfigOptions = {
    initFocus,
    initReconnect
};

const IS_REACT_LEGACY = !react.useId;
const IS_SERVER = !isWindowDefined || isLegacyDeno;
// Polyfill requestAnimationFrame
const rAF = (f)=>hasRequestAnimationFrame() ? window['requestAnimationFrame'](f) : setTimeout(f, 1);
// React currently throws a warning when using useLayoutEffect on the server.
// To get around it, we can conditionally useEffect on the server (no-op) and
// useLayoutEffect in the browser.
const useIsomorphicLayoutEffect = IS_SERVER ? react.useEffect : react.useLayoutEffect;
// This assignment is to extend the Navigator type to use effectiveType.
const navigatorConnection = typeof navigator !== 'undefined' && navigator.connection;
// Adjust the config based on slow connection status (<= 70Kbps).
const slowConnection = !IS_SERVER && navigatorConnection && ([
    'slow-2g',
    '2g'
].includes(navigatorConnection.effectiveType) || navigatorConnection.saveData);

// use WeakMap to store the object->key mapping
// so the objects can be garbage collected.
// WeakMap uses a hashtable under the hood, so the lookup
// complexity is almost O(1).
const table = new WeakMap();
const getTypeName = (value)=>OBJECT.prototype.toString.call(value);
const isObjectTypeName = (typeName, type)=>typeName === `[object ${type}]`;
// counter of the key
let counter = 0;
// A stable hash implementation that supports:
// - Fast and ensures unique hash properties
// - Handles unserializable values
// - Handles object key ordering
// - Generates short results
//
// This is not a serialization function, and the result is not guaranteed to be
// parsable.
const stableHash = (arg)=>{
    const type = typeof arg;
    const typeName = getTypeName(arg);
    const isDate = isObjectTypeName(typeName, 'Date');
    const isRegex = isObjectTypeName(typeName, 'RegExp');
    const isPlainObject = isObjectTypeName(typeName, 'Object');
    let result;
    let index;
    if (OBJECT(arg) === arg && !isDate && !isRegex) {
        // Object/function, not null/date/regexp. Use WeakMap to store the id first.
        // If it's already hashed, directly return the result.
        result = table.get(arg);
        if (result) return result;
        // Store the hash first for circular reference detection before entering the
        // recursive `stableHash` calls.
        // For other objects like set and map, we use this id directly as the hash.
        result = ++counter + '~';
        table.set(arg, result);
        if (Array.isArray(arg)) {
            // Array.
            result = '@';
            for(index = 0; index < arg.length; index++){
                result += stableHash(arg[index]) + ',';
            }
            table.set(arg, result);
        }
        if (isPlainObject) {
            // Object, sort keys.
            result = '#';
            const keys = OBJECT.keys(arg).sort();
            while(!isUndefined(index = keys.pop())){
                if (!isUndefined(arg[index])) {
                    result += index + ':' + stableHash(arg[index]) + ',';
                }
            }
            table.set(arg, result);
        }
    } else {
        result = isDate ? arg.toJSON() : type == 'symbol' ? arg.toString() : type == 'string' ? JSON.stringify(arg) : '' + arg;
    }
    return result;
};

const serialize = (key)=>{
    if (isFunction(key)) {
        try {
            key = key();
        } catch (err) {
            // dependencies not ready
            key = '';
        }
    }
    // Use the original key as the argument of fetcher. This can be a string or an
    // array of values.
    const args = key;
    // If key is not falsy, or not an empty array, hash it.
    key = typeof key == 'string' ? key : (Array.isArray(key) ? key.length : key) ? stableHash(key) : '';
    return [
        key,
        args
    ];
};

// Global timestamp.
let __timestamp = 0;
const getTimestamp = ()=>++__timestamp;

async function internalMutate(...args) {
    const [cache, _key, _data, _opts] = args;
    // When passing as a boolean, it's explicitly used to disable/enable
    // revalidation.
    const options = mergeObjects({
        populateCache: true,
        throwOnError: true
    }, typeof _opts === 'boolean' ? {
        revalidate: _opts
    } : _opts || {});
    let populateCache = options.populateCache;
    const rollbackOnErrorOption = options.rollbackOnError;
    let optimisticData = options.optimisticData;
    const rollbackOnError = (error)=>{
        return typeof rollbackOnErrorOption === 'function' ? rollbackOnErrorOption(error) : rollbackOnErrorOption !== false;
    };
    const throwOnError = options.throwOnError;
    // If the second argument is a key filter, return the mutation results for all
    // filtered keys.
    if (isFunction(_key)) {
        const keyFilter = _key;
        const matchedKeys = [];
        const it = cache.keys();
        for (const key of it){
            if (// Skip the special useSWRInfinite and useSWRSubscription keys.
            !/^\$(inf|sub)\$/.test(key) && keyFilter(cache.get(key)._k)) {
                matchedKeys.push(key);
            }
        }
        return Promise.all(matchedKeys.map(mutateByKey));
    }
    return mutateByKey(_key);
    async function mutateByKey(_k) {
        // Serialize key
        const [key] = serialize(_k);
        if (!key) return;
        const [get, set] = createCacheHelper(cache, key);
        const [EVENT_REVALIDATORS, MUTATION, FETCH, PRELOAD] = SWRGlobalState.get(cache);
        const startRevalidate = ()=>{
            const revalidators = EVENT_REVALIDATORS[key];
            const revalidate = isFunction(options.revalidate) ? options.revalidate(get().data, _k) : options.revalidate !== false;
            if (revalidate) {
                // Invalidate the key by deleting the concurrent request markers so new
                // requests will not be deduped.
                delete FETCH[key];
                delete PRELOAD[key];
                if (revalidators && revalidators[0]) {
                    return revalidators[0](MUTATE_EVENT).then(()=>get().data);
                }
            }
            return get().data;
        };
        // If there is no new data provided, revalidate the key with current state.
        if (args.length < 3) {
            // Revalidate and broadcast state.
            return startRevalidate();
        }
        let data = _data;
        let error;
        let isError = false;
        // Update global timestamps.
        const beforeMutationTs = getTimestamp();
        MUTATION[key] = [
            beforeMutationTs,
            0
        ];
        const hasOptimisticData = !isUndefined(optimisticData);
        const state = get();
        // `displayedData` is the current value on screen. It could be the optimistic value
        // that is going to be overridden by a `committedData`, or get reverted back.
        // `committedData` is the validated value that comes from a fetch or mutation.
        const displayedData = state.data;
        const currentData = state._c;
        const committedData = isUndefined(currentData) ? displayedData : currentData;
        // Do optimistic data update.
        if (hasOptimisticData) {
            optimisticData = isFunction(optimisticData) ? optimisticData(committedData, displayedData) : optimisticData;
            // When we set optimistic data, backup the current committedData data in `_c`.
            set({
                data: optimisticData,
                _c: committedData
            });
        }
        if (isFunction(data)) {
            // `data` is a function, call it passing current cache value.
            try {
                data = data(committedData);
            } catch (err) {
                // If it throws an error synchronously, we shouldn't update the cache.
                error = err;
                isError = true;
            }
        }
        // `data` is a promise/thenable, resolve the final data first.
        if (data && isPromiseLike(data)) {
            // This means that the mutation is async, we need to check timestamps to
            // avoid race conditions.
            data = await data.catch((err)=>{
                error = err;
                isError = true;
            });
            // Check if other mutations have occurred since we've started this mutation.
            // If there's a race we don't update cache or broadcast the change,
            // just return the data.
            if (beforeMutationTs !== MUTATION[key][0]) {
                if (isError) throw error;
                return data;
            } else if (isError && hasOptimisticData && rollbackOnError(error)) {
                // Rollback. Always populate the cache in this case but without
                // transforming the data.
                populateCache = true;
                // Reset data to be the latest committed data, and clear the `_c` value.
                set({
                    data: committedData,
                    _c: UNDEFINED
                });
            }
        }
        // If we should write back the cache after request.
        if (populateCache) {
            if (!isError) {
                // Transform the result into data.
                if (isFunction(populateCache)) {
                    const populateCachedData = populateCache(data, committedData);
                    set({
                        data: populateCachedData,
                        error: UNDEFINED,
                        _c: UNDEFINED
                    });
                } else {
                    // Only update cached data and reset the error if there's no error. Data can be `undefined` here.
                    set({
                        data,
                        error: UNDEFINED,
                        _c: UNDEFINED
                    });
                }
            }
        }
        // Reset the timestamp to mark the mutation has ended.
        MUTATION[key][1] = getTimestamp();
        // Update existing SWR Hooks' internal states:
        Promise.resolve(startRevalidate()).then(()=>{
            // The mutation and revalidation are ended, we can clear it since the data is
            // not an optimistic value anymore.
            set({
                _c: UNDEFINED
            });
        });
        // Throw error or return data
        if (isError) {
            if (throwOnError) throw error;
            return;
        }
        return data;
    }
}

const revalidateAllKeys = (revalidators, type)=>{
    for(const key in revalidators){
        if (revalidators[key][0]) revalidators[key][0](type);
    }
};
const initCache = (provider, options)=>{
    // The global state for a specific provider will be used to deduplicate
    // requests and store listeners. As well as a mutate function that is bound to
    // the cache.
    // The provider's global state might be already initialized. Let's try to get the
    // global state associated with the provider first.
    if (!SWRGlobalState.has(provider)) {
        const opts = mergeObjects(defaultConfigOptions, options);
        // If there's no global state bound to the provider, create a new one with the
        // new mutate function.
        const EVENT_REVALIDATORS = Object.create(null);
        const mutate = internalMutate.bind(UNDEFINED, provider);
        let unmount = noop;
        const subscriptions = Object.create(null);
        const subscribe = (key, callback)=>{
            const subs = subscriptions[key] || [];
            subscriptions[key] = subs;
            subs.push(callback);
            return ()=>{
                const index = subs.indexOf(callback);
                if (index >= 0) {
                    // O(1): swap with the last element and pop
                    subs[index] = subs[subs.length - 1];
                    subs.pop();
                }
            };
        };
        const setter = (key, value, prev)=>{
            provider.set(key, value);
            const subs = subscriptions[key];
            if (subs) {
                for (const fn of subs){
                    fn(value, prev);
                }
            }
        };
        const initProvider = ()=>{
            if (!SWRGlobalState.has(provider)) {
                // Update the state if it's new, or if the provider has been extended.
                SWRGlobalState.set(provider, [
                    EVENT_REVALIDATORS,
                    Object.create(null),
                    Object.create(null),
                    Object.create(null),
                    mutate,
                    setter,
                    subscribe
                ]);
                if (!IS_SERVER) {
                    // When listening to the native events for auto revalidations,
                    // we intentionally put a delay (setTimeout) here to make sure they are
                    // fired after immediate JavaScript executions, which can be
                    // React's state updates.
                    // This avoids some unnecessary revalidations such as
                    // https://github.com/vercel/swr/issues/1680.
                    const releaseFocus = opts.initFocus(setTimeout.bind(UNDEFINED, revalidateAllKeys.bind(UNDEFINED, EVENT_REVALIDATORS, FOCUS_EVENT)));
                    const releaseReconnect = opts.initReconnect(setTimeout.bind(UNDEFINED, revalidateAllKeys.bind(UNDEFINED, EVENT_REVALIDATORS, RECONNECT_EVENT)));
                    unmount = ()=>{
                        // eslint-disable-next-line @typescript-eslint/no-unused-expressions
                        releaseFocus && releaseFocus();
                        // eslint-disable-next-line @typescript-eslint/no-unused-expressions
                        releaseReconnect && releaseReconnect();
                        // When un-mounting, we need to remove the cache provider from the state
                        // storage too because it's a side-effect. Otherwise, when re-mounting we
                        // will not re-register those event listeners.
                        SWRGlobalState.delete(provider);
                    };
                }
            }
        };
        initProvider();
        // This is a new provider, we need to initialize it and setup DOM events
        // listeners for `focus` and `reconnect` actions.
        // We might want to inject an extra layer on top of `provider` in the future,
        // such as key serialization, auto GC, etc.
        // For now, it's just a `Map` interface without any modifications.
        return [
            provider,
            mutate,
            initProvider,
            unmount
        ];
    }
    return [
        provider,
        SWRGlobalState.get(provider)[4]
    ];
};

// error retry
const onErrorRetry = (_, __, config, revalidate, opts)=>{
    const maxRetryCount = config.errorRetryCount;
    const currentRetryCount = opts.retryCount;
    // Exponential backoff
    const timeout = ~~((Math.random() + 0.5) * (1 << (currentRetryCount < 8 ? currentRetryCount : 8))) * config.errorRetryInterval;
    if (!isUndefined(maxRetryCount) && currentRetryCount > maxRetryCount) {
        return;
    }
    setTimeout(revalidate, timeout, opts);
};
const compare = dequal;
// Default cache provider
const [cache, mutate] = initCache(new Map());
// Default config
const defaultConfig = mergeObjects({
    // events
    onLoadingSlow: noop,
    onSuccess: noop,
    onError: noop,
    onErrorRetry,
    onDiscarded: noop,
    // switches
    revalidateOnFocus: true,
    revalidateOnReconnect: true,
    revalidateIfStale: true,
    shouldRetryOnError: true,
    // timeouts
    errorRetryInterval: slowConnection ? 10000 : 5000,
    focusThrottleInterval: 5 * 1000,
    dedupingInterval: 2 * 1000,
    loadingTimeout: slowConnection ? 5000 : 3000,
    // providers
    compare,
    isPaused: ()=>false,
    cache,
    mutate,
    fallback: {}
}, // use web preset by default
preset);

const mergeConfigs = (a, b)=>{
    // Need to create a new object to avoid mutating the original here.
    const v = mergeObjects(a, b);
    // If two configs are provided, merge their `use` and `fallback` options.
    if (b) {
        const { use: u1, fallback: f1 } = a;
        const { use: u2, fallback: f2 } = b;
        if (u1 && u2) {
            v.use = u1.concat(u2);
        }
        if (f1 && f2) {
            v.fallback = mergeObjects(f1, f2);
        }
    }
    return v;
};

const SWRConfigContext = (0,react.createContext)({});
const SWRConfig = (props)=>{
    const { value } = props;
    const parentConfig = (0,react.useContext)(SWRConfigContext);
    const isFunctionalConfig = isFunction(value);
    const config = (0,react.useMemo)(()=>isFunctionalConfig ? value(parentConfig) : value, [
        isFunctionalConfig,
        parentConfig,
        value
    ]);
    // Extend parent context values and middleware.
    const extendedConfig = (0,react.useMemo)(()=>isFunctionalConfig ? config : mergeConfigs(parentConfig, config), [
        isFunctionalConfig,
        parentConfig,
        config
    ]);
    // Should not use the inherited provider.
    const provider = config && config.provider;
    // initialize the cache only on first access.
    const cacheContextRef = (0,react.useRef)(UNDEFINED);
    if (provider && !cacheContextRef.current) {
        cacheContextRef.current = initCache(provider(extendedConfig.cache || cache), config);
    }
    const cacheContext = cacheContextRef.current;
    // Override the cache if a new provider is given.
    if (cacheContext) {
        extendedConfig.cache = cacheContext[0];
        extendedConfig.mutate = cacheContext[1];
    }
    // Unsubscribe events.
    useIsomorphicLayoutEffect(()=>{
        if (cacheContext) {
            // eslint-disable-next-line @typescript-eslint/no-unused-expressions
            cacheContext[2] && cacheContext[2]();
            return cacheContext[3];
        }
    }, []);
    return (0,react.createElement)(SWRConfigContext.Provider, mergeObjects(props, {
        value: extendedConfig
    }));
};



;// ./node_modules/swr/dist/_internal/constants.mjs
const INFINITE_PREFIX = '$inf$';



;// ./node_modules/swr/dist/_internal/index.mjs
/* unused harmony import specifier */ var _internal_IS_SERVER;
/* unused harmony import specifier */ var _internal_serialize;
/* unused harmony import specifier */ var _internal_SWRGlobalState;
/* unused harmony import specifier */ var _internal_cache;









// @ts-expect-error
const enableDevtools = isWindowDefined && window.__SWR_DEVTOOLS_USE__;
const use = enableDevtools ? window.__SWR_DEVTOOLS_USE__ : [];
const setupDevTools = ()=>{
    if (enableDevtools) {
        // @ts-expect-error
        window.__SWR_DEVTOOLS_REACT__ = react;
    }
};

const normalize = (args)=>{
    return isFunction(args[1]) ? [
        args[0],
        args[1],
        args[2] || {}
    ] : [
        args[0],
        null,
        (args[1] === null ? args[2] : args[1]) || {}
    ];
};

const useSWRConfig = ()=>{
    const parentConfig = (0,react.useContext)(SWRConfigContext);
    const mergedConfig = (0,react.useMemo)(()=>mergeObjects(defaultConfig, parentConfig), [
        parentConfig
    ]);
    return mergedConfig;
};

const preload = (key_, fetcher)=>{
    // preload should be a no-op on the server
    if (_internal_IS_SERVER) {
        return undefined;
    }
    const [key, fnArg] = _internal_serialize(key_);
    const [, , , PRELOAD] = _internal_SWRGlobalState.get(_internal_cache);
    // Prevent preload to be called multiple times before used.
    if (PRELOAD[key]) return PRELOAD[key];
    const req = fetcher(fnArg);
    PRELOAD[key] = req;
    return req;
};
const middleware = (useSWRNext)=>(key_, fetcher_, config)=>{
        // fetcher might be a sync function, so this should not be an async function
        const fetcher = fetcher_ && ((...args)=>{
            const [key] = serialize(key_);
            const [, , , PRELOAD] = SWRGlobalState.get(cache);
            if (key.startsWith(INFINITE_PREFIX)) {
                // we want the infinite fetcher to be called.
                // handling of the PRELOAD cache happens there.
                return fetcher_(...args);
            }
            const req = PRELOAD[key];
            if (isUndefined(req)) return fetcher_(...args);
            delete PRELOAD[key];
            return req;
        });
        return useSWRNext(key_, fetcher, config);
    };

const BUILT_IN_MIDDLEWARE = use.concat(middleware);

// It's tricky to pass generic types as parameters, so we just directly override
// the types here.
const withArgs = (hook)=>{
    return function useSWRArgs(...args) {
        // Get the default and inherited configuration.
        const fallbackConfig = useSWRConfig();
        // Normalize arguments.
        const [key, fn, _config] = normalize(args);
        // Merge configurations.
        const config = mergeConfigs(fallbackConfig, _config);
        // Apply middleware
        let next = hook;
        const { use } = config;
        const middleware = (use || []).concat(BUILT_IN_MIDDLEWARE);
        for(let i = middleware.length; i--;){
            next = middleware[i](next);
        }
        return next(key, fn || config.fetcher || null, config);
    };
};

// Add a callback function to a list of keyed callback functions and return
// the unsubscribe function.
const subscribeCallback = (key, callbacks, callback)=>{
    const keyedRevalidators = callbacks[key] || (callbacks[key] = []);
    keyedRevalidators.push(callback);
    return ()=>{
        const index = keyedRevalidators.indexOf(callback);
        if (index >= 0) {
            // O(1): faster than splice
            keyedRevalidators[index] = keyedRevalidators[keyedRevalidators.length - 1];
            keyedRevalidators.pop();
        }
    };
};

// Create a custom hook with a middleware
const withMiddleware = (useSWR, middleware)=>{
    return (...args)=>{
        const [key, fn, config] = normalize(args);
        const uses = (config.use || []).concat(middleware);
        return useSWR(key, fn, {
            ...config,
            use: uses
        });
    };
};

setupDevTools();



;// ./node_modules/swr/dist/index/index.mjs





// Shared state between server components and client components
const index_noop = ()=>{};
// Using noop() as the undefined value as undefined can be replaced
// by something else. Prettier ignore and extra parentheses are necessary here
// to ensure that tsc doesn't remove the __NOINLINE__ comment.
// prettier-ignore
const index_UNDEFINED = /*#__NOINLINE__*/ index_noop();
const index_OBJECT = (/* unused pure expression or super */ null && (Object));
const index_isUndefined = (v)=>v === index_UNDEFINED;
const index_isFunction = (v)=>typeof v == 'function';

// use WeakMap to store the object->key mapping
// so the objects can be garbage collected.
// WeakMap uses a hashtable under the hood, so the lookup
// complexity is almost O(1).
const index_table = new WeakMap();
const index_getTypeName = (value)=>index_OBJECT.prototype.toString.call(value);
const index_isObjectTypeName = (typeName, type)=>typeName === `[object ${type}]`;
// counter of the key
let index_counter = 0;
// A stable hash implementation that supports:
// - Fast and ensures unique hash properties
// - Handles unserializable values
// - Handles object key ordering
// - Generates short results
//
// This is not a serialization function, and the result is not guaranteed to be
// parsable.
const index_stableHash = (arg)=>{
    const type = typeof arg;
    const typeName = index_getTypeName(arg);
    const isDate = index_isObjectTypeName(typeName, 'Date');
    const isRegex = index_isObjectTypeName(typeName, 'RegExp');
    const isPlainObject = index_isObjectTypeName(typeName, 'Object');
    let result;
    let index;
    if (index_OBJECT(arg) === arg && !isDate && !isRegex) {
        // Object/function, not null/date/regexp. Use WeakMap to store the id first.
        // If it's already hashed, directly return the result.
        result = index_table.get(arg);
        if (result) return result;
        // Store the hash first for circular reference detection before entering the
        // recursive `stableHash` calls.
        // For other objects like set and map, we use this id directly as the hash.
        result = ++index_counter + '~';
        index_table.set(arg, result);
        if (Array.isArray(arg)) {
            // Array.
            result = '@';
            for(index = 0; index < arg.length; index++){
                result += index_stableHash(arg[index]) + ',';
            }
            index_table.set(arg, result);
        }
        if (isPlainObject) {
            // Object, sort keys.
            result = '#';
            const keys = index_OBJECT.keys(arg).sort();
            while(!index_isUndefined(index = keys.pop())){
                if (!index_isUndefined(arg[index])) {
                    result += index + ':' + index_stableHash(arg[index]) + ',';
                }
            }
            index_table.set(arg, result);
        }
    } else {
        result = isDate ? arg.toJSON() : type == 'symbol' ? arg.toString() : type == 'string' ? JSON.stringify(arg) : '' + arg;
    }
    return result;
};

const index_serialize = (key)=>{
    if (index_isFunction(key)) {
        try {
            key = key();
        } catch (err) {
            // dependencies not ready
            key = '';
        }
    }
    // Use the original key as the argument of fetcher. This can be a string or an
    // array of values.
    const args = key;
    // If key is not falsy, or not an empty array, hash it.
    key = typeof key == 'string' ? key : (Array.isArray(key) ? key.length : key) ? index_stableHash(key) : '';
    return [
        key,
        args
    ];
};

const unstable_serialize = (key)=>index_serialize(key)[0];

/// <reference types="react/experimental" />
const index_use = react.use || // This extra generic is to avoid TypeScript mixing up the generic and JSX sytax
// and emitting an error.
// We assume that this is only for the `use(thenable)` case, not `use(context)`.
// https://github.com/facebook/react/blob/aed00dacfb79d17c53218404c52b1c7aa59c4a89/packages/react-server/src/ReactFizzThenable.js#L45
((thenable)=>{
    switch(thenable.status){
        case 'pending':
            throw thenable;
        case 'fulfilled':
            return thenable.value;
        case 'rejected':
            throw thenable.reason;
        default:
            thenable.status = 'pending';
            thenable.then((v)=>{
                thenable.status = 'fulfilled';
                thenable.value = v;
            }, (e)=>{
                thenable.status = 'rejected';
                thenable.reason = e;
            });
            throw thenable;
    }
});
const WITH_DEDUPE = {
    dedupe: true
};
// React can only unwrap a thenable synchronously when it has React's thenable
// status fields, so this fulfilled no-op keeps Suspense paths from waiting.
const resolvedUndef = Promise.resolve(UNDEFINED);
// @ts-ignore modify react promise status
resolvedUndef.status = 'fulfilled';
// @ts-ignore modify react promise value
resolvedUndef.value = UNDEFINED;
const sub = ()=>noop;
/**
 * The core implementation of the useSWR hook.
 *
 * This is the main handler function that implements all SWR functionality including
 * data fetching, caching, revalidation, error handling, and state management.
 * It manages the complete lifecycle of SWR requests from initialization through
 * cleanup.
 *
 * Key responsibilities:
 * - Key serialization and normalization
 * - Cache state management and synchronization
 * - Automatic and manual revalidation
 * - Error handling and retry logic
 * - Suspense integration
 * - Loading state management
 * - Effect cleanup and memory management
 *
 * @template Data - The type of data returned by the fetcher
 * @template Error - The type of error that can be thrown
 *
 * @param _key - The SWR key (string, array, object, function, or falsy)
 * @param fetcher - The fetcher function to retrieve data, or null to disable fetching
 * @param config - Complete SWR configuration object with both public and internal options
 *
 * @returns SWRResponse object containing data, error, mutate function, and loading states
 *
 * @internal This is the internal implementation. Use `useSWR` instead.
 */ const useSWRHandler = (_key, fetcher, config)=>{
    const { cache, compare, suspense, fallbackData, revalidateOnMount, revalidateIfStale, refreshInterval, refreshWhenHidden, refreshWhenOffline, keepPreviousData, strictServerPrefetchWarning } = config;
    const [EVENT_REVALIDATORS, MUTATION, FETCH, PRELOAD] = SWRGlobalState.get(cache);
    // `key` is the identifier of the SWR internal state,
    // `fnArg` is the argument/arguments parsed from the key, which will be passed
    // to the fetcher.
    // All of them are derived from `_key`.
    const [key, fnArg] = serialize(_key);
    // If it's the initial render of this hook.
    const initialMountedRef = (0,react.useRef)(false);
    // If the hook is unmounted already. This will be used to prevent some effects
    // to be called after unmounting.
    const unmountedRef = (0,react.useRef)(false);
    // Refs to keep the key and config.
    const keyRef = (0,react.useRef)(key);
    const fetcherRef = (0,react.useRef)(fetcher);
    const configRef = (0,react.useRef)(config);
    const getConfig = ()=>configRef.current;
    const isActive = ()=>getConfig().isVisible() && getConfig().isOnline();
    const [getCache, setCache, subscribeCache, getInitialCache] = createCacheHelper(cache, key);
    const stateDependencies = (0,react.useRef)({}).current;
    // Resolve the fallback data from either the inline option, or the global provider.
    // If it's a promise, we simply let React suspend and resolve it for us.
    const fallback = isUndefined(fallbackData) ? isUndefined(config.fallback) ? UNDEFINED : config.fallback[key] : fallbackData;
    const isEqual = (prev, current)=>{
        for(const _ in stateDependencies){
            const t = _;
            if (t === 'data') {
                if (!compare(prev[t], current[t])) {
                    if (!isUndefined(prev[t])) {
                        return false;
                    }
                    if (!compare(returnedData, current[t])) {
                        return false;
                    }
                }
            } else {
                if (current[t] !== prev[t]) {
                    return false;
                }
            }
        }
        return true;
    };
    const isInitialMount = !initialMountedRef.current;
    const getSnapshot = (0,react.useMemo)(()=>{
        const cachedData = getCache();
        const initialData = getInitialCache();
        const getSelectedCache = (state)=>{
            // We only select the needed fields from the state.
            const snapshot = mergeObjects(state);
            delete snapshot._k;
            const shouldStartRequest = (()=>{
                if (!key) return false;
                if (!fetcher) return false;
                // If it's paused, we skip revalidation.
                if (getConfig().isPaused()) return false;
                // If `revalidateOnMount` is set, we take the value directly.
                if (isInitialMount && !isUndefined(revalidateOnMount)) return revalidateOnMount;
                const data = !isUndefined(fallback) ? fallback : snapshot.data;
                if (suspense) return isUndefined(data) || revalidateIfStale;
                return isUndefined(data) || revalidateIfStale;
            })();
            if (!shouldStartRequest) {
                return snapshot;
            }
            return {
                isValidating: true,
                isLoading: true,
                ...snapshot
            };
        };
        const clientSnapshot = getSelectedCache(cachedData);
        const serverSnapshot = cachedData === initialData ? clientSnapshot : getSelectedCache(initialData);
        // To make sure that we are returning the same object reference to avoid
        // unnecessary re-renders, we keep the previous snapshot and use deep
        // comparison to check if we need to return a new one.
        let memorizedSnapshot = clientSnapshot;
        return [
            ()=>{
                const newSnapshot = getSelectedCache(getCache());
                const compareResult = isEqual(newSnapshot, memorizedSnapshot);
                if (compareResult) {
                    // Mentally, we should always return the `memorizedSnapshot` here
                    // as there's no change between the new and old snapshots.
                    // However, since the `isEqual` function only compares selected fields,
                    // the values of the unselected fields might be changed. That's
                    // simply because we didn't track them.
                    // To support the case in https://github.com/vercel/swr/pull/2576,
                    // we need to update these fields in the `memorizedSnapshot` too
                    // with direct mutations to ensure the snapshot is always up-to-date
                    // even for the unselected fields, but only trigger re-renders when
                    // the selected fields are changed.
                    memorizedSnapshot.data = newSnapshot.data;
                    memorizedSnapshot.isLoading = newSnapshot.isLoading;
                    memorizedSnapshot.isValidating = newSnapshot.isValidating;
                    memorizedSnapshot.error = newSnapshot.error;
                    return memorizedSnapshot;
                } else {
                    memorizedSnapshot = newSnapshot;
                    return newSnapshot;
                }
            },
            ()=>serverSnapshot
        ];
    // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [
        cache,
        key
    ]);
    // Get the current state that SWR should return.
    const cached = (0,shim.useSyncExternalStore)((0,react.useCallback)((callback)=>subscribeCache(key, (current, prev)=>{
            if (!isEqual(prev, current)) callback();
        }), // eslint-disable-next-line react-hooks/exhaustive-deps
    [
        cache,
        key
    ]), getSnapshot[0], getSnapshot[1]);
    const hasRevalidator = EVENT_REVALIDATORS[key] && EVENT_REVALIDATORS[key].length > 0;
    const cachedData = cached.data;
    const data = isUndefined(cachedData) ? fallback && isPromiseLike(fallback) ? index_use(fallback) : fallback : cachedData;
    const error = cached.error;
    // Use a ref to store previously returned data. Use the initial data as its initial value.
    const laggyDataRef = (0,react.useRef)(data);
    const returnedData = keepPreviousData ? isUndefined(cachedData) ? isUndefined(laggyDataRef.current) ? data : laggyDataRef.current : cachedData : data;
    const hasKeyButNoData = key && isUndefined(data);
    const hydrationRef = (0,react.useRef)(null);
    // Note: the conditionally hook call is fine because the environment
    // `IS_SERVER` never changes.
    // @ts-expect-error -- use hydrationRef directly
    !IS_SERVER && // getServerSnapshot is only called during hydration
    // eslint-disable-next-line react-hooks/rules-of-hooks
    (0,shim.useSyncExternalStore)(sub, ()=>{
        hydrationRef.current = false;
        return hydrationRef;
    }, ()=>{
        hydrationRef.current = true;
        return hydrationRef;
    });
    const isHydration = hydrationRef.current;
    // During the initial SSR render, warn if the key has no data pre-fetched via:
    // - fallback data
    // - preload calls
    // - initial data from the cache provider
    // We only warn once for each key during Hydration.
    if (strictServerPrefetchWarning && isHydration && !suspense && hasKeyButNoData) {
        console.warn(`Missing pre-initiated data for serialized key "${key}" during server-side rendering. Data fetching should be initiated on the server and provided to SWR via fallback data. You can set "strictServerPrefetchWarning: false" to disable this warning.`);
    }
    // Resolve the default validating state:
    // If it's able to validate, and it should revalidate when mount, this will be true.
    // - Suspense mode and there's stale data for the initial render.
    // - Not suspense mode and there is no fallback data and `revalidateIfStale` is enabled.
    // - `revalidateIfStale` is enabled but `data` is not defined.
    const shouldDoInitialRevalidation = (()=>{
        if (!key || !fetcher) return false;
        // If it's paused, we skip revalidation.
        if (getConfig().isPaused()) return false;
        // if a key already has revalidators and also has error, we should not trigger revalidation
        if (hasRevalidator && !isUndefined(error)) return false;
        // If `revalidateOnMount` is set, we take the value directly.
        if (isInitialMount && !isUndefined(revalidateOnMount)) return revalidateOnMount;
        // Under suspense mode, it will always fetch on render if there is no
        // stale data so no need to revalidate immediately mount it again.
        // If data exists, only revalidate if `revalidateIfStale` is true.
        if (suspense) return isUndefined(data) ? false : revalidateIfStale;
        // If there is no stale data, we need to revalidate when mount;
        // If `revalidateIfStale` is set to true, we will always revalidate.
        return isUndefined(data) || revalidateIfStale;
    })();
    const defaultValidatingState = isInitialMount && shouldDoInitialRevalidation;
    const isValidating = isUndefined(cached.isValidating) ? defaultValidatingState : cached.isValidating;
    const isLoading = isUndefined(cached.isLoading) ? defaultValidatingState : cached.isLoading;
    // The revalidation function is a carefully crafted wrapper of the original
    // `fetcher`, to correctly handle the many edge cases.
    const revalidate = (0,react.useCallback)(async (revalidateOpts)=>{
        const currentFetcher = fetcherRef.current;
        if (!key || !currentFetcher || unmountedRef.current || getConfig().isPaused()) {
            return false;
        }
        let newData;
        let startAt;
        let loading = true;
        const opts = revalidateOpts || {};
        // If there is no ongoing concurrent request, or `dedupe` is not set, a
        // new request should be initiated.
        const shouldStartNewRequest = !FETCH[key] || !opts.dedupe;
        /*
         For React 17
         Do unmount check for calls:
         If key has changed during the revalidation, or the component has been
         unmounted, old dispatch and old event callbacks should not take any
         effect

        For React 18
        only check if key has changed
        https://github.com/reactwg/react-18/discussions/82
      */ const callbackSafeguard = ()=>{
            if (IS_REACT_LEGACY) {
                return !unmountedRef.current && key === keyRef.current && initialMountedRef.current;
            }
            return key === keyRef.current;
        };
        // The final state object when the request finishes.
        const finalState = {
            isValidating: false,
            isLoading: false
        };
        const finishRequestAndUpdateState = ()=>{
            setCache(finalState);
        };
        const cleanupState = ()=>{
            // Check if it's still the same request before deleting it.
            const requestInfo = FETCH[key];
            if (requestInfo && requestInfo[1] === startAt) {
                delete FETCH[key];
            }
        };
        // Start fetching. Change the `isValidating` state, update the cache.
        const initialState = {
            isValidating: true
        };
        // It is in the `isLoading` state, if and only if there is no cached data.
        // This bypasses fallback data and laggy data.
        if (isUndefined(getCache().data)) {
            initialState.isLoading = true;
        }
        try {
            if (shouldStartNewRequest) {
                setCache(initialState);
                // If no cache is being rendered currently (it shows a blank page),
                // we trigger the loading slow event.
                if (config.loadingTimeout && isUndefined(getCache().data)) {
                    setTimeout(()=>{
                        if (loading && callbackSafeguard()) {
                            getConfig().onLoadingSlow(key, config);
                        }
                    }, config.loadingTimeout);
                }
                // Start the request and save the timestamp.
                // Key must be truthy if entering here.
                FETCH[key] = [
                    currentFetcher(fnArg),
                    getTimestamp()
                ];
            }
            // Wait until the ongoing request is done. Deduplication is also
            // considered here.
            ;
            [newData, startAt] = FETCH[key];
            newData = await newData;
            if (shouldStartNewRequest) {
                // If the request isn't interrupted, clean it up after the
                // deduplication interval.
                setTimeout(cleanupState, config.dedupingInterval);
            }
            // If there're other ongoing request(s), started after the current one,
            // we need to ignore the current one to avoid possible race conditions:
            //   req1------------------>res1        (current one)
            //        req2---------------->res2
            // the request that fired later will always be kept.
            // The timestamp maybe be `undefined` or a number
            if (!FETCH[key] || FETCH[key][1] !== startAt) {
                if (shouldStartNewRequest) {
                    if (callbackSafeguard()) {
                        getConfig().onDiscarded(key);
                    }
                }
                return false;
            }
            // Clear error.
            finalState.error = UNDEFINED;
            // If there're other mutations(s), that overlapped with the current revalidation:
            // case 1:
            //   req------------------>res
            //       mutate------>end
            // case 2:
            //         req------------>res
            //   mutate------>end
            // case 3:
            //   req------------------>res
            //       mutate-------...---------->
            // we have to ignore the revalidation result (res) because it's no longer fresh.
            // meanwhile, a new revalidation should be triggered when the mutation ends.
            const mutationInfo = MUTATION[key];
            if (!isUndefined(mutationInfo) && // case 1
            (startAt <= mutationInfo[0] || // case 2
            startAt <= mutationInfo[1] || // case 3
            mutationInfo[1] === 0)) {
                finishRequestAndUpdateState();
                if (shouldStartNewRequest) {
                    if (callbackSafeguard()) {
                        getConfig().onDiscarded(key);
                    }
                }
                return false;
            }
            // Deep compare with the latest state to avoid extra re-renders.
            // For local state, compare and assign.
            const cacheData = getCache().data;
            // Since the compare fn could be custom fn
            // cacheData might be different from newData even when compare fn returns True
            finalState.data = compare(cacheData, newData) ? cacheData : newData;
            // Trigger the successful callback if it's the original request.
            if (shouldStartNewRequest) {
                if (callbackSafeguard()) {
                    getConfig().onSuccess(newData, key, config);
                }
            }
        } catch (err) {
            cleanupState();
            const currentConfig = getConfig();
            const { shouldRetryOnError } = currentConfig;
            // Not paused, we continue handling the error. Otherwise, discard it.
            if (!currentConfig.isPaused()) {
                // Get a new error, don't use deep comparison for errors.
                finalState.error = err;
                // Error event and retry logic. Only for the actual request, not
                // deduped ones.
                if (shouldStartNewRequest && callbackSafeguard()) {
                    currentConfig.onError(err, key, currentConfig);
                    if (shouldRetryOnError === true || isFunction(shouldRetryOnError) && shouldRetryOnError(err)) {
                        if (!getConfig().revalidateOnFocus || !getConfig().revalidateOnReconnect || isActive()) {
                            // If it's inactive, stop. It will auto-revalidate when
                            // refocusing or reconnecting.
                            // When retrying, deduplication is always enabled.
                            currentConfig.onErrorRetry(err, key, currentConfig, (_opts)=>{
                                const revalidators = EVENT_REVALIDATORS[key];
                                if (revalidators && revalidators[0]) {
                                    revalidators[0](ERROR_REVALIDATE_EVENT, _opts);
                                }
                            }, {
                                retryCount: (opts.retryCount || 0) + 1,
                                dedupe: true
                            });
                        }
                    }
                }
            }
        }
        // Mark loading as stopped.
        loading = false;
        // Update the current hook's state.
        finishRequestAndUpdateState();
        return true;
    }, // `setState` is immutable, and `eventsCallback`, `fnArg`, and
    // `keyValidating` are depending on `key`, so we can exclude them from
    // the deps array.
    //
    // FIXME:
    // `fn` and `config` might be changed during the lifecycle,
    // but they might be changed every render like this.
    // `useSWR('key', () => fetch('/api/'), { suspense: true })`
    // So we omit the values from the deps array
    // even though it might cause unexpected behaviors.
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
        key,
        cache
    ]);
    // Similar to the global mutate but bound to the current cache and key.
    // `cache` isn't allowed to change during the lifecycle.
    const boundMutate = (0,react.useCallback)(// Use callback to make sure `keyRef.current` returns latest result every time
    (...args)=>{
        return internalMutate(cache, keyRef.current, ...args);
    }, // eslint-disable-next-line react-hooks/exhaustive-deps
    []);
    // The logic for updating refs.
    useIsomorphicLayoutEffect(()=>{
        fetcherRef.current = fetcher;
        configRef.current = config;
        // Handle laggy data updates. If there's cached data of the current key,
        // it'll be the correct reference.
        if (!isUndefined(cachedData)) {
            laggyDataRef.current = cachedData;
        }
    });
    // After mounted or key changed.
    useIsomorphicLayoutEffect(()=>{
        if (!key) return;
        const softRevalidate = revalidate.bind(UNDEFINED, WITH_DEDUPE);
        let nextFocusRevalidatedAt = 0;
        if (getConfig().revalidateOnFocus) {
            const initNow = Date.now();
            nextFocusRevalidatedAt = initNow + getConfig().focusThrottleInterval;
        }
        // Expose revalidators to global event listeners. So we can trigger
        // revalidation from the outside.
        const onRevalidate = (type, opts = {})=>{
            if (type == FOCUS_EVENT) {
                const now = Date.now();
                if (getConfig().revalidateOnFocus && now > nextFocusRevalidatedAt && isActive()) {
                    nextFocusRevalidatedAt = now + getConfig().focusThrottleInterval;
                    softRevalidate();
                }
            } else if (type == RECONNECT_EVENT) {
                if (getConfig().revalidateOnReconnect && isActive()) {
                    softRevalidate();
                }
            } else if (type == MUTATE_EVENT) {
                return revalidate();
            } else if (type == ERROR_REVALIDATE_EVENT) {
                return revalidate(opts);
            }
            return;
        };
        const unsubEvents = subscribeCallback(key, EVENT_REVALIDATORS, onRevalidate);
        // Mark the component as mounted and update corresponding refs.
        unmountedRef.current = false;
        keyRef.current = key;
        initialMountedRef.current = true;
        // Keep the original key in the cache.
        setCache({
            _k: fnArg
        });
        // Trigger a revalidation
        if (shouldDoInitialRevalidation) {
            // Performance optimization: if a request is already in progress for this key,
            // skip the revalidation to avoid redundant work
            if (!FETCH[key]) {
                if (isUndefined(data) || IS_SERVER) {
                    // Revalidate immediately.
                    softRevalidate();
                } else {
                    // Delay the revalidate if we have data to return so we won't block
                    // rendering.
                    rAF(softRevalidate);
                }
            }
        }
        return ()=>{
            // Mark it as unmounted.
            unmountedRef.current = true;
            unsubEvents();
        };
    }, [
        key
    ]);
    // Polling
    useIsomorphicLayoutEffect(()=>{
        let timer;
        function next() {
            // Use the passed interval
            // ...or invoke the function with the updated data to get the interval
            const interval = isFunction(refreshInterval) ? refreshInterval(getCache().data) : refreshInterval;
            // We only start the next interval if `refreshInterval` is not 0, and:
            // - `force` is true, which is the start of polling
            // - or `timer` is not 0, which means the effect wasn't canceled
            if (interval && timer !== -1) {
                timer = setTimeout(execute, interval);
            }
        }
        function execute() {
            // Check if it's OK to execute:
            // Only revalidate when the page is visible, online, and not errored.
            if (!getCache().error && (refreshWhenHidden || getConfig().isVisible()) && (refreshWhenOffline || getConfig().isOnline())) {
                revalidate(WITH_DEDUPE).then(next);
            } else {
                // Schedule the next interval to check again.
                next();
            }
        }
        next();
        return ()=>{
            if (timer) {
                clearTimeout(timer);
                timer = -1;
            }
        };
    }, [
        refreshInterval,
        refreshWhenHidden,
        refreshWhenOffline,
        key
    ]);
    // Display debug info in React DevTools.
    (0,react.useDebugValue)(returnedData);
    // In Suspense mode, we can't return the empty `data` state.
    // If there is an `error`, the `error` needs to be thrown to the error boundary.
    // If there is no `error`, the `revalidation` promise needs to be thrown to
    // the suspense boundary.
    if (suspense) {
        // SWR should throw when trying to use Suspense on the server with React 18,
        // without providing any fallback data. This causes hydration errors. See:
        // https://github.com/vercel/swr/issues/1832
        if (!IS_REACT_LEGACY && IS_SERVER && hasKeyButNoData) {
            throw new Error('Fallback data is required when using Suspense in SSR.');
        }
        // Always update fetcher and config refs even with the Suspense mode.
        if (hasKeyButNoData) {
            fetcherRef.current = fetcher;
            configRef.current = config;
            unmountedRef.current = false;
        }
        const req = PRELOAD[key];
        const mutateReq = !isUndefined(req) && hasKeyButNoData ? boundMutate(req) : resolvedUndef;
        index_use(mutateReq);
        if (!isUndefined(error) && hasKeyButNoData) {
            throw error;
        }
        const revalidation = hasKeyButNoData ? revalidate(WITH_DEDUPE) : resolvedUndef;
        if (!isUndefined(returnedData) && hasKeyButNoData) {
            // @ts-ignore modify react promise status
            revalidation.status = 'fulfilled';
            // @ts-ignore modify react promise value
            revalidation.value = true;
        }
        index_use(revalidation);
    }
    const swrResponse = {
        mutate: boundMutate,
        get data () {
            stateDependencies.data = true;
            return returnedData;
        },
        get error () {
            stateDependencies.error = true;
            return error;
        },
        get isValidating () {
            stateDependencies.isValidating = true;
            return isValidating;
        },
        get isLoading () {
            stateDependencies.isLoading = true;
            return isLoading;
        }
    };
    return swrResponse;
};
const index_SWRConfig = OBJECT.defineProperty(SWRConfig, 'defaultValue', {
    value: defaultConfig
});
/**
 * A hook to fetch data.
 *
 * @see {@link https://swr.vercel.app}
 *
 * @example
 * ```jsx
 * import useSWR from 'swr'
 * function Profile() {
 *   const { data, error, isLoading } = useSWR('/api/user', fetcher)
 *   if (error) return <div>failed to load</div>
 *   if (isLoading) return <div>loading...</div>
 *   return <div>hello {data.name}!</div>
 * }
 * ```
 */ const useSWR = withArgs(useSWRHandler);

// useSWR



;// ./node_modules/react-tweet/dist/api/fetch-tweet.js
const SYNDICATION_URL = 'https://cdn.syndication.twimg.com';
class TwitterApiError extends Error {
    constructor({ message, status, data }){
        super(message);
        this.name = 'TwitterApiError';
        this.status = status;
        this.data = data;
    }
}
const TWEET_ID = /^[0-9]+$/;
function getToken(id) {
    return (Number(id) / 1e15 * Math.PI).toString(6 ** 2).replace(/(0+|\.)/g, '');
}
/**
 * Fetches a tweet from the Twitter syndication API.
 */ async function fetchTweet(id, fetchOptions) {
    var _res_headers_get;
    if (id.length > 40 || !TWEET_ID.test(id)) {
        throw new Error(`Invalid tweet id: ${id}`);
    }
    const url = new URL(`${SYNDICATION_URL}/tweet-result`);
    url.searchParams.set('id', id);
    url.searchParams.set('lang', 'en');
    url.searchParams.set('features', [
        'tfw_timeline_list:',
        'tfw_follower_count_sunset:true',
        'tfw_tweet_edit_backend:on',
        'tfw_refsrc_session:on',
        'tfw_fosnr_soft_interventions_enabled:on',
        'tfw_show_birdwatch_pivots_enabled:on',
        'tfw_show_business_verified_badge:on',
        'tfw_duplicate_scribes_to_settings:on',
        'tfw_use_profile_image_shape_enabled:on',
        'tfw_show_blue_verified_badge:on',
        'tfw_legacy_timeline_sunset:true',
        'tfw_show_gov_verified_badge:on',
        'tfw_show_business_affiliate_badge:on',
        'tfw_tweet_edit_frontend:on'
    ].join(';'));
    url.searchParams.set('token', getToken(id));
    const res = await fetch(url.toString(), fetchOptions);
    const isJson = (_res_headers_get = res.headers.get('content-type')) == null ? void 0 : _res_headers_get.includes('application/json');
    const data = isJson ? await res.json() : undefined;
    if (res.ok) {
        if ((data == null ? void 0 : data.__typename) === 'TweetTombstone') {
            return {
                tombstone: true
            };
        }
        if (data && Object.keys(data).length === 0) {
            return {
                notFound: true
            };
        }
        return {
            data
        };
    }
    if (res.status === 404) {
        return {
            notFound: true
        };
    }
    throw new TwitterApiError({
        message: typeof data.error === 'string' ? data.error : `Failed to fetch tweet at "${url}" with "${res.status}".`,
        status: res.status,
        data
    });
}

;// ./node_modules/react-tweet/dist/hooks.js
/* unused harmony import specifier */ var useState;
/* unused harmony import specifier */ var useEffect;
'use client';



// Avoids an error when used in the pages directory where useSWR might be in `default`.
const hooks_useSWR = useSWR["default"] || useSWR;
const host = 'https://react-tweet.vercel.app';
async function fetcher([url, fetchOptions]) {
    const res = await fetch(url, fetchOptions);
    const json = await res.json();
    // We return null in case `json.data` is undefined, that way we can check for "loading" by
    // checking if data is `undefined`. `null` means it was fetched.
    if (res.ok) return json.data || null;
    throw new TwitterApiError({
        message: `Failed to fetch tweet at "${url}" with "${res.status}".`,
        data: json,
        status: res.status
    });
}
/**
 * SWR hook for fetching a tweet in the browser.
 */ const useTweet = (id, apiUrl, fetchOptions)=>{
    const { isLoading, data, error } = hooks_useSWR(()=>apiUrl || id ? [
            apiUrl || id && `${host}/api/tweet/${id}`,
            fetchOptions
        ] : null, fetcher, {
        revalidateIfStale: false,
        revalidateOnFocus: false,
        shouldRetryOnError: false
    });
    return {
        // If data is `undefined` then it might be the first render where SWR hasn't started doing
        // any work, so we set `isLoading` to `true`.
        isLoading: Boolean(isLoading || data === undefined && !error),
        data,
        error
    };
};
const useMounted = ()=>{
    const [mounted, setMounted] = useState(false);
    useEffect(()=>setMounted(true), []);
    return mounted;
};

;// ./node_modules/react-tweet/dist/swr.js
'use client';



const Tweet = ({ id, apiUrl, fallback = /*#__PURE__*/ (0,jsx_runtime.jsx)(TweetSkeleton, {}), components, fetchOptions, onError })=>{
    const { data, error, isLoading } = useTweet(id, apiUrl, fetchOptions);
    if (isLoading) return fallback;
    if (error || !data) {
        const NotFound = (components == null ? void 0 : components.TweetNotFound) || TweetNotFound;
        return /*#__PURE__*/ (0,jsx_runtime.jsx)(NotFound, {
            error: onError ? onError(error) : error
        });
    }
    return /*#__PURE__*/ (0,jsx_runtime.jsx)(EmbeddedTweet, {
        tweet: data,
        components: components
    });
};


/***/ }

}]);