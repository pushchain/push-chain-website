# robots.txt for core.push.org
#
# A temporary showcase of the new build, served on a subdomain of push.org.
# Nothing here should be indexed against push.org, so nothing here is crawlable.

User-agent: *
Disallow: /
